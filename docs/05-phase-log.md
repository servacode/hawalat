# مشروع حوالات — سجل المراحل وفحص القبول (Phase Log & Acceptance Audit)

> لكل مرحلة من خطة البناء (`03-build-plan.md`) قسم هنا يوثّق:
> **الهدف → ما أُنجز → فحص القبول ضد قواعدنا → القرار (مقبولة/مرفوضة) → ملاحظات مؤجلة.**
> لا تُعتبر أي مرحلة **مغلقة** إلا بعد اجتياز فحصها وتوثيقه هنا.

---

## 📏 دستور الفحص الثابت (يُطبَّق على كل مرحلة)

كل مرحلة تُفحص ضد هذه القواعد — ما ينطبق منها على نطاقها:

| # | القاعدة | مصدرها |
|---|---|---|
| ق1 | **المركزية الكاملة:** لا لون/خط/حجم/ثابت مكتوب يدوياً خارج طبقة التوكِنز والثوابت المركزية | الخطة §1 |
| ق2 | **عزل المستأجرين:** كل بيان مالي محكوم بـ tenant، ولا استعلام يتجاوز العزل | المبادئ |
| ق3 | **محاسبة سليمة:** قيد مزدوج متوازن دائماً، `Decimal` فقط، **لا حذف** (عكس قيد) | الدورة §0 |
| ق4 | **RBAC:** كل نقطة وصول محكومة بالدور (أدمن/كبير/صغير) | الجزء 6 |
| ق5 | **RTL وعربي أولاً:** الواجهة عربية RTL، الأرقام جدولية للمال | الهوية |
| ق6 | **الوضعان فاتح/داكن** يعملان من نفس التوكِنز | الهوية |
| ق7 | **الاختبارات:** كل منطق جوهري له اختبار آلي يمرّ | الخطة §5 |
| ق8 | **الجودة:** lint + format نظيفان، CI أخضر | الخطة م0 |
| ق9 | **قابلية التشغيل:** البيئة تعمل بأمر واحد وبالوضعين (Docker/محلي) | الخطة م0 |
| ق10 | **التوثيق:** ما بُني موثّق، وما تغيّر عن الخطة مسجّل بسببه | هذا الملف |

---

## المرحلة 0 — الأساسات (Foundations)

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07
- **الهدف المعلن:** بيئة تعمل بأمر واحد: Django + Next.js + Docker + CI + أدوات جودة.

### ما أُنجز
- Backend: Django 5.1 + DRF + Channels + Celery، إعدادات مقسّمة (base/dev/prod)، تطبيق `core`، نقطة `/api/health/`، اختبار دخاني.
- Frontend: Next.js 15 + TS + Tailwind 4، عربي/RTL، خط Tajawal، نواة طبقة التوكِنز بألوان الهوية.
- بنية: docker-compose (db+redis+web+worker+frontend)، Dockerfiles، Makefile، pre-commit، GitHub Actions CI، `.env.example` يخدم الوضعين.

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق1 المركزية | ✅ | الألوان في `globals.css` كطبقة توكِنز (`--brand-600`، `--accent`...) والصفحة التمهيدية تستهلك `bg-brand`/`text-muted` فقط — لا hex يدوي في أي مكوّن. الخط عبر متغيّر `--font-tajawal` |
| ق2 العزل | ➖ لا ينطبق بعد | لا نماذج بيانات مالية في هذه المرحلة — يبدأ فرضه في المرحلة 1 (`TenantScopedModel`) |
| ق3 المحاسبة | ➖ لا ينطبق بعد | محرك القيود يبدأ في المرحلة 1/4 |
| ق4 RBAC | ➖ لا ينطبق بعد | DRF مضبوط افتراضياً على `IsAuthenticated` (أساس آمن)، التفصيل في المرحلة 2 |
| ق5 RTL | ✅ | `<html lang="ar" dir="rtl">` من الجذر + `font-feature-settings: "tnum"` للأرقام |
| ق6 فاتح/داكن | ✅ | التوكِنز معرّفة للوضعين عبر `prefers-color-scheme` في نفس الطبقة |
| ق7 الاختبارات | ✅ | اختبار دخاني لنقطة الصحة يمرّ (`manage.py test` → OK) |
| ق8 الجودة | ✅ | `ruff check` + `ruff format --check` نظيفان، `npm run lint` نظيف، CI مُعدّ للطرفين |
| ق9 التشغيل | ✅ | `docker compose config` سليم؛ محلياً بلا Docker: sqlite تلقائي والاختبارات تمرّ. **ملاحظة:** تشغيل compose الفعلي غير ممكن في بيئة العمل السحابية (لا daemon) — **يُتحقق منه على جهاز المالك** |
| ق10 التوثيق | ✅ | قسم "التشغيل" في README + هذا السجل |

### ملاحظات وديون مؤجلة
1. **تشغيل `docker compose up` الفعلي** لم يُختبر إلا بنيوياً (لا Docker daemon في بيئة العمل) — يُجرَّب على جهاز المالك أول مرة.
2. طبقة التوكِنز الحالية **نواة مختصرة** — التوسعة الكاملة (كل تدرّجات الهوية والمقاييس والظلال) هي صلب المرحلة 1.
3. الصفحة الرئيسية placeholder مؤقت — تُستبدل ببوابة الدخول في المرحلة 2.
4. لا يوجد PR بعد: المستودع بلا فرع أساسي (`main`) للمقارنة — يُفتح PR عند إنشائه.

---

## المرحلة 1 — النظام المركزي (Centralized Systems) ⭐

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07
- **الهدف المعلن:** طبقة التوكِنز + مكتبة المكوّنات + صفحة عرض؛ وفي الباكند: Tenancy + RBAC + هيكل محرك القيود + Mixins.

### ما أُنجز

**Backend:**
- `apps/core/tenancy.py`: ContextVar + `TenantManager` (فلترة تلقائية) + `tenant_context` + `TenantMiddleware`.
- `apps/core/models.py`: `Tenant`, `TenantScopedModel` (إسناد تلقائي للمستأجر), `Account` (أنواع: مكتب صغير/وسيط/محل/ذاتي/أرباح), `JournalEntry`/`JournalLine` (Decimal 18,4 + قيد DB يمنع سطراً صفرياً/سالباً + حذف يرمي خطأ), `AuditLog`.
- `apps/core/services.py`: `post_entry` (توازن لكل عملة، Decimal حصراً، حسابات نفس المستأجر، ذرّي) و`reverse_entry` (مرآة + وسم + منع العكس المزدوج).
- `apps/accounts/models.py`: مستخدم مخصص (`AUTH_USER_MODEL`) بدور (admin/big_office/small_office) + مستأجر + كود مكتب + حقول واتساب + حظر، مع تحقق: الأدمن بلا مستأجر والمكاتب بمستأجر إلزامي.
- `apps/core/permissions.py`: صلاحيات DRF للأدوار الثلاثة.

**Frontend:**
- `globals.css`: طبقة التوكِنز الكاملة (تدرّج العلامة 50-900، ذهبي، دلالية، pos/neg، محايدات، ظلال، زوايا) للوضعين عبر `@theme`.
- `components/ui/`: Button (4 variants × 3 sizes)، Badge (6 حالات الحركة بأسماء مركزية)، Card، Input، Select، Table (تمرير أفقي آمن)، StatCard (بدلالة pos/neg/profit)، EmptyState، Skeleton، Modal (Esc + وصولية)، Tabs — تصدير مركزي واحد.
- `lib/`: `cn`، `format` (formatMoney/balanceTone/formatDateTime — كل عرض مالي يمرّ منها)، `api` (عميل موحّد + ApiError)، `i18n/ar` (كل النصوص مركزية).
- صفحة `/design` (معرض المكوّنات) + PWA manifest + أيقونة العلامة.

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق1 المركزية | ✅ | صفر hex في المكوّنات/الصفحات (كلها أصناف توكِنز)؛ النصوص في `i18n/ar`؛ التنسيق المالي في `format.ts` حصراً؛ تصدير مركزي واحد للمكتبة |
| ق2 العزل | ✅ | اختبارات `TenantIsolationTests`: السياق يفلتر (t1 لا يرى t2)، إسناد تلقائي للمستأجر — تمرّ |
| ق3 المحاسبة | ✅ | `LedgerEngineTests` (8 اختبارات): مثال الدورة (1000/10/15→ربح 5) يتوازن صفرياً، رفض غير المتوازن/floats/عابر المستأجرين/متعدد العملات غير المتوازن، **لا حذف** (قيد وسطر)، العكس يعيد الأرصدة صفراً، منع العكس المزدوج |
| ق4 RBAC | ✅ | مستخدم مخصص بدور + `UserRoleTests` (أدمن بلا مستأجر/مكتب بمستأجر) + صلاحيات DRF جاهزة |
| ق5 RTL | ✅ | لقطة `docs/screenshots/design-light.png`: كل شيء RTL بخط Tajawal وأرقام جدولية |
| ق6 فاتح/داكن | ✅ | لقطتا `design-light.png` و`design-dark.png` من نفس التوكِنز — كلاهما سليم بصرياً |
| ق7 الاختبارات | ✅ | **15 اختباراً تمرّ** (`manage.py test` → OK) |
| ق8 الجودة | ✅ | `ruff check` نظيف، `ruff format --check` نظيف، `eslint` نظيف، `next build` ناجح |
| ق9 التشغيل | ✅ | الترحيلات تعمل؛ صفحة `/design` قُدّمت فعلياً على خادم production وصُوّرت |
| ق10 التوثيق | ✅ | هذا القسم + تعليقات عربية وافية في الكود + لقطات شاشة كدليل |

### ملاحظات وديون مؤجلة
1. مكوّنات مؤجلة عمداً لمراحل لاحقة عند أول حاجة فعلية: Toast، Pagination، DatePicker، CurrencyInput (الأساس جاهز لإضافتها بنفس النمط).
2. `AUTH_USER_MODEL` ثُبّت الآن عمداً (قرار معماري: تغييره لاحقاً مؤلم) — تقديم جزئي من المرحلة 2 بسبب تقني موثّق.
3. صلاحيات DRF للأدوار لم تُستخدم بعد في أي view — تُفعّل مع أول endpoints (المرحلة 2).
4. تكامل JWT (نقاط الدخول/التحديث) في المرحلة 2.

---

## المرحلة 2 — المصادقة والهوية (Auth & Identity) 🔑

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07
- **الهدف المعلن:** دخول موحّد + تذكّرني + استعادة كلمة المرور + الأكواد الفريدة + توجيه حسب الدور + تسجيل ذاتي معطّل.

### ما أُنجز

**Backend (`apps/accounts`):**
- **دخول موحّد JWT** (`/api/auth/login/`): يضيف للتوكن claims الدور والمستأجر وكود المكتب، يمنع المحظورين والمستأجرين الموقوفين، ويعيد بيانات المستخدم.
- **تذكّرني:** يرفع عمر refresh من يوم إلى 30 يوماً.
- **فرض الحظر على كل طلب** (`BlockAwareJWTAuthentication`) حتى مع توكن صالح — مضبوط افتراضياً في DRF.
- `/me/` و`/refresh/` و`/change-password/` (بالتحقق من الحالية).
- **استعادة هرمية** (`/reset-password/`): الكبير ← صغار مستأجره فقط؛ الأدمن ← الكبار فقط (لا علاقة له بالصغار)؛ الصغير ← لا أحد. كل استعادة تُسجَّل في AuditLog.
- **الأكواد الفريدة** (`services.py`): `BIGnnn` للكبار، و`BIGnnn-SMLmmm` للصغار (الكود يحمل هوية الكبير — عزل)، مع `create_big_office`/`create_small_office` ذرّيتين.
- **التسجيل الذاتي** (`/register/`): خلف مفتاح `PlatformSettings.self_registration_enabled` (معطّل افتراضياً)؛ عند التفعيل ينشئ مستأجراً **موقوفاً** بانتظار تفعيل الأدمن؛ يتطلب الموافقة على الشروط وتطابق كلمتي المرور.
- نموذج `PlatformSettings` (صف واحد): الوضع المجاني + مفتاح التسجيل.

**Frontend:**
- صفحة `/login` بالمكوّنات المركزية: تذكّرني، «نسيت كلمة المرور؟» (نافذة تشرح الاستعادة الهرمية)، رسائل خطأ واضحة.
- `lib/auth.ts`: جلسة مركزية (تذكّرني → localStorage، وإلا sessionStorage) + `roleHome`.
- `RoleGuard`: يحمي الصفحات ويعيد كل دور لواجهته + زر خروج.
- ثلاث صفحات محمية (`/admin`، `/office`، `/small`) والجذر يوجّه تلقائياً.

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق1 المركزية | ✅ | صفحة الدخول والصفحات المحمية كلها من مكتبة المكوّنات والتوكِنز؛ الجلسة والتوجيه في `lib/auth.ts` وحده |
| ق2 العزل | ✅ | اختبار `test_big_cannot_reset_foreign_small`: الكبير مُنع (403) من استعادة كلمة مرور صغير تابع لمستأجر آخر |
| ق3 المحاسبة | ➖ | لا قيود جديدة في هذه المرحلة |
| ق4 RBAC | ✅ | claims الدور في التوكن؛ الاستعادة الهرمية مفحوصة للأدوار الثلاثة؛ E2E: الصغير مُنع من `/admin` وأُعيد لواجهته |
| ق5 RTL | ✅ | لقطة `login-page.png`: عربية RTL كاملة |
| ق6 فاتح/داكن | ✅ | نفس التوكِنز — لا ألوان جديدة خارجها |
| ق7 الاختبارات | ✅ | **37 اختبار باكند تمرّ** + **5 فحوص E2E متصفح حقيقي** (دخول كبير→/office، صغير→/small، منع عابر للأدوار، خطأ كلمة المرور) |
| ق8 الجودة | ✅ | ruff نظيف، eslint نظيف، next build ناجح |
| ق9 التشغيل | ✅ | E2E جرى على الخادمين الفعليين (Django 8000 + Next 3000) بتواصل CORS حقيقي |
| ق10 التوثيق | ✅ | هذا القسم + لقطتا `login-page.png` و`office-home.png` |

### ملاحظات وديون مؤجلة
1. **تسريع اختبارات:** hasher خفيف أثناء `manage.py test` فقط (لا يمسّ الإنتاج) — موثّق في dev.py.
2. تسجيل الخروج لا يُبطل الـ refresh token على الخادم (blacklist) — يُضاف في مرحلة الأمان (11) إن لزم.
3. حماية الواجهة client-side فقط (كافية الآن) — الحماية الفعلية للبيانات في الـ API؛ حماية SSR/middleware تُقيَّم لاحقاً.
4. التسجيل الذاتي يغطي المكاتب الكبيرة فقط (الصغار يفتحهم الكبير حصراً — متسق مع هرمية فتح الحسابات).

---

## المرحلة 3 — وحدة الأدمن (Admin Module) 👑

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07
- **الهدف المعلن:** إدارة المكاتب الكبيرة + الباقات + الاشتراكات (تفعيل يدوي) + مفتاح الوضع المجاني + إحصاءات غير مالية + بث + تدقيق.

### ما أُنجز

**Backend (`apps/admin_panel`):**
- **الباقات:** حدّان (مكاتب + حركات) + سعر + مدة؛ «الحذف» = تعطيل فقط (اتساقاً مع لا-حذف).
- **الاشتراكات:** المكتب يطلب → دفع يدوي خارج النظام → الأدمن يفعّل (يحسب الانتهاء) أو يرفض؛ منع طلبين معلّقين.
- **قاعدة الترحيل (المشهد 1):** `can_create_small_office` — الوضع المجاني مفتوح؛ المدفوع يحتسب فقط المكاتب المُنشأة **بعد** تفعيل الاشتراك.
- **إدارة المكاتب الكبيرة:** عرض (بلا أي حقل مالي — مفحوص بالاختبار)، فتح بكود مولّد، تعديل، **حظر/تفعيل يقطع وصول كل مستخدمي المستأجر فوراً** حتى مع توكن صالح (فُرض في طبقة المصادقة).
- **إعدادات المنصة:** مفتاحا الوضع المجاني والتسجيل الذاتي (GET/PATCH).
- **الإحصاءات:** أرقام مجرّدة فقط (مكاتب/صغار/طلبات معلّقة/تنتهي قريباً/عدد حركات).
- **البث:** تنبيهات الأدمن → المكاتب الكبيرة فقط، ونقطة قراءة للمكتب الكبير.
- **التدقيق:** كل فعل أدمن يُسجَّل + نقطة عرض آخر 100 سجل.
- جانب المكتب الكبير: عرض الباقات المتاحة واشتراكاته + طلب باقة.

**Frontend (لوحة `/admin` كاملة):**
- `Shell` مركزي (شريط جانبي RTL + ترويسة) قابل لإعادة الاستخدام لكل الأدوار.
- `authedApi` مركزي (توكن تلقائي + 401 → خروج).
- 6 صفحات: الرئيسية (إحصاءات)، المكاتب (جدول + إنشاء + حظر/تفعيل)، الباقات (جدول + إنشاء)، الاشتراكات (تبويبا معلّقة/أرشيف + تفعيل/رفض)، التنبيهات (بث + قائمة)، الإعدادات (مفتاحان).

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق1 المركزية | ✅ | كل الصفحات من المكتبة والتوكِنز؛ Shell وauthedApi مركزيان يعاد استخدامهما للأدوار القادمة |
| ق2 العزل | ✅ | الأدمن يرى المستأجرين إدارياً فقط؛ اختبار يؤكد **صفر حقول مالية** في قوائم الأدمن وإحصاءاته |
| ق3 المحاسبة | ➖ | لا قيود مالية في هذه المرحلة (الحذف=تعطيل للباقات اتساقاً مع المبدأ) |
| ق4 RBAC | ✅ | اختبارات: الكبير ممنوع من 5 نقاط أدمن (403)، الأدمن ممنوع من نقاط المكتب، والمجهول 401 |
| ق5 RTL | ✅ | لقطات `admin-dashboard.png` و`admin-offices.png` — لوحة RTL كاملة |
| ق6 فاتح/داكن | ✅ | لا ألوان جديدة خارج التوكِنز |
| ق7 الاختبارات | ✅ | **52 اختبار باكند** (منها: الترحيل بسيناريو 5 قديمة + حد 2، الحظر الفوري بتوكن صالح، دورة طلب→تفعيل) + **4 فحوص E2E متصفح** (لوحة، إنشاء BIG002 من الواجهة، إنشاء باقة، الإعدادات) |
| ق8 الجودة | ✅ | ruff + eslint + next build نظيفة |
| ق9 التشغيل | ✅ | E2E على الخادمين الفعليين |
| ق10 التوثيق | ✅ | هذا القسم + لقطتا شاشة |

### ملاحظات وديون مؤجلة
1. واجهة «طلب الباقة» على جانب المكتب الكبير (الـ API جاهز ومختبَر) — تُبنى مع لوحة المكتب الكبير.
2. تنبيهات «القراءة/غير المقروءة» للبث — مع نظام الإشعارات (المرحلة 8).
3. انتهاء الاشتراك تلقائياً (مهمة دورية Celery) — مع مرحلة التشغيل؛ حالياً `expires_at` يُفحص عند الاستعلام.
4. عمود «عدد الحركات» في الإحصاءات = 0 حتى مرحلة الحركات.

---

## المرحلة 4 — النواة المحاسبية (Accounting Core) ⭐

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07
- **الهدف المعلن:** العملات + صناديق الوسطاء والمحل + حسابات الصغار + الحدود السالبة + التسويات + عكس القيد — بأرصدة صحيحة تُختبَر آلياً.

### ما أُنجز

**تصحيحان جوهريان اكتشفتهما هذه المرحلة:**
1. **ضبط اصطلاح الأرصدة على السرد:** `balance = مدين − دائن`. صندوق: موجب = رصيدنا فيه؛ **الإيداع يزيده وتمرير الحركة ينقصه** (حرفياً كالمشهدين 2 و5)؛ صغير: موجب = عليه؛ الربح دخل دائن يُعرض معكوساً. حُدّثت الوثيقة 02 §0 و§4 واختبارات المرحلة 1، مع اختبار السيناريو السردي: إيداع 2000 − حركة 1010 = **990** ✓.
2. **🔴 ثغرة عزل حقيقية اكتشفتها الاختبارات:** مصادقة JWT تحدث بعد Django middleware، فسياق المستأجر لم يكن يُضبط عبر الـ API (مستأجر رأى عملات غيره!). **الإصلاح:** ضبط السياق في `BlockAwareJWTAuthentication.get_user` (والـ middleware للتنظيف). اختبارا العزل عبر API يثبتان الإصلاح.

**تطبيق `apps/boxes`:**
- **Currency**: عملات لكل مستأجر يديرها الكبير (uniq لكل مستأجر).
- **IntermediaryBox**: اسم + رقم + عملات M2M (الجزء 12) — حساب دفتر أستاذ لكل (صندوق × عملة) يُنشأ عند أول حاجة.
- **SmallOfficeAccount / ShopCashAccount / FeesProfit**: توفير حسابات تلقائي لكل عملة.
- **CreditLimit** (الجزء 5): حد سالب لكل (صغير × عملة) + `check_credit_limit` بالرياضيات الصحيحة، و**التعزيز يفتح المجال** (مختبَر).
- **التسويات (المشهد 5):** `deposit_to_box` (مدين الصندوق/دائن الصغير) و`withdraw_from_box` (العكس) — عبر محرك القيود حصراً.
- **API كامل للكبير:** عملات CRUD، صناديق (+أرصدة لكل عملة +كشف +اعتماد +سحب)، حدود، صندوق المحل (أرصدة+كشف)، **عكس قيد** لأي قيد ضمن مستأجره.
- **API للصغير:** `/api/small/balances/` — له/عليه/الصافي لكل عملة (الجزء 3-د).
- كل فعل مالي في سجل التدقيق.

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق1 المركزية | ✅ | كل قيد عبر `post_entry` حصراً؛ توفير الحسابات في services وحدها؛ لا SQL/قيود متناثرة |
| ق2 العزل | ✅ | إصلاح ثغرة السياق + 3 اختبارات API: مستأجر يرى عملاته فقط، لا يسوّي على صغير غيره (400)، لا يعكس قيد غيره (404) |
| ق3 المحاسبة | ✅ | الاصطلاح موحّد وموثّق؛ إيداع/سحب/حركة/عكس كلها متوازنة؛ سيناريو 2000−1010=990 السردي مختبَر؛ Decimal حصراً |
| ق4 RBAC | ✅ | الصغير ممنوع من كل نقاط إدارة الصناديق (403، مختبَر)؛ الإدارة للكبير حصراً |
| ق5-ق6 | ➖ | لا واجهة في هذه المرحلة (تُبنى في 5-6 فوق هذه الـ APIs) |
| ق7 الاختبارات | ✅ | **66 اختباراً تمرّ** (+14 جديداً: تسويات، حدود مع تعزيز، تدفق API كامل، عزل، كشوف) |
| ق8 الجودة | ✅ | ruff نظيف بالكامل |
| ق9 التشغيل | ✅ | الترحيلات سليمة، التدفق الكامل عبر API حقيقي مختبَر (عملة→صندوق→اعتماد→كشف→عكس→رصيد صفر) |
| ق10 التوثيق | ✅ | تحديث 02 §0/§4 بالاصطلاح النهائي + هذا القسم |

### ملاحظات وديون مؤجلة
1. واجهات الصناديق/الكشوف تُبنى في المرحلتين 5-6 فوق هذه الـ APIs الجاهزة.
2. «الوجهة» و«رأس مال الأجور/المستحقة» يدخلان مع نموذج الحركة (المرحلة 5) — `check_credit_limit` جاهزة للاستدعاء عند القبول.
3. كشف الحساب حالياً بآخر 200 سطر — الفلترة الزمنية والتصدير مع المرحلتين 6-7.

---

## المرحلة 5 — الحركات (Transactions) 🔄

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07
- **الهدف المعلن:** الدورة الكاملة للحركة (إنشاء→قبول/رفض→دفع/تسليم→تعديل/عكس) بواجهات الطرفين وقيود دقيقة.

### ما أُنجز

**Backend (`apps/transactions`):**
- **النموذج الكامل:** مرجع فريد `HW-BIGnnn-nnnnnn` مرتبط بكود المكتب، مرسِل/مستفيد/وجهة (نص حر)، عملتا قبض/تسليم + سعر صرف مثبّت + مبلغ مسلَّم، أجور (رأس مال/مستحقة)، 3 حالات مستقلة، روابط قيود القبول والدفع.
- **قرار تقني موثّق:** الأجور **بالعملة المقبوضة** (حسم سؤال الدورة §8).
- **قيد القبول أحادي العملة** = المشهد 2 حرفياً؛ **ثنائي العملة عبر حسابات مقاصة صرف** لكل عملة → متوازن لكل عملة مع مركز صرف قابل للتدقيق (نوع حساب جديد FX_CLEARING).
- **فحوص قبل القبول:** اكتمال الأجور + سعر الصرف إن اختلفت + **الحد السالب** (يمنع برسالة «تعزيز الرصيد» والحركة تبقى معلّقة).
- مدفوعة (§4-ب): نقد للمحل ويصفّي دين الصغير. تسليم: حالة فقط. رفض: بلا قيود. **عكس كامل** (يعكس الدفع ثم القبول). **تعديل** عبر عكس+إعادة ترحيل.
- **حد حركات الباقة** مع الترحيل (المشهد 1) + ربط عدّاد الأدمن.
- قوائم بفلتر موحّد (حالات/عملة/تاريخ/بحث/منشئ): جارية/سجل للكبير، وسجل خاص للصغير. **الكبير يُنشئ لنفسه من نفس النقاط** (المشهد 3).

**Frontend:**
- **الصغير:** رئيسية (صناديقه لكل عملة + آخر الحركات)، **إرسال حركة** (تنبيه ثنائية العملة، وبعد الحفظ **زر واتساب** ينسخ النص المُقولب ويفتح رابط مجموعته — قالب مركزي في `lib/whatsapp.ts`)، سجل بفلتر.
- **الكبير:** رئيسية (عدّاد الجارية)، **الحركات الجارية** بنافذة معالجة كاملة (صناديق مؤهّلة بعملة التسليم فقط + أجور + سعر صرف إلزامي عند الاختلاف + **عرض الربح حياً** + قبول معطّل حتى الاكتمال)، **سجل** بأزرار مدفوعة/تسليم/عكس.

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق1 المركزية | ✅ | كل الأفعال في services حصراً؛ قالب الواتساب مركزي؛ الفلاتر دالة واحدة؛ الواجهات من المكتبة |
| ق2 العزل | ✅ | الصغير يرى حركاته فقط (مختبَر)؛ كل الاستعلامات عبر المدير المفلتر |
| ق3 المحاسبة | ✅ | E2E فعلي ثم فحص القاعدة: الوعد −20، حلب −985، المحل +1015، الربح 10 — **مطابقة 100%** لحسابة يدوية لحركتين+دفعة فوق إيداع 2000؛ ثنائية العملة متوازنة لكل عملة (مختبَرة: 1000 TRY×400 = 400000 SYP مع مقاصّتين) |
| ق4 RBAC | ✅ | الصغير ممنوع من كل أفعال المعالجة (403 × 5 أفعال، مختبَر) |
| ق5 RTL | ✅ | 5 لقطات شاشة (إرسال، جارية، معالجة، سجل) |
| ق6 | ✅ | لا ألوان خارج التوكِنز |
| ق7 الاختبارات | ✅ | **83 اختبار باكند** (+17: مشهد 2 الحرفي 990، ثنائية عملة، حد يمنع القبول، دفع مزدوج ممنوع، عكس شامل، حد الباقة بالترحيل، فلاتر) + **E2E متصفح للدورة الكاملة**: صغير يرسل → كبير يعالج (الوعد، 10/15) → مدفوعة → تم التسليم → الصغير يرى النتيجة |
| ق8 الجودة | ✅ | ruff + eslint + build نظيفة |
| ق9 التشغيل | ✅ | الدورة الكاملة على خوادم حقيقية |
| ق10 التوثيق | ✅ | هذا القسم + قرار عملة الأجور موثّق |

### ملاحظات وديون مؤجلة
1. زر الواتساب مع **رابط مجموعة**: ينسخ النص ثم يفتح المجموعة (واتساب لا يدعم نصاً مسبقاً للمجموعات)؛ بلا رابط → نافذة مشاركة بالنص. وضع **البوت** في مرحلته (9).
2. تحديث القوائم حالياً بإعادة تحميل — **اللحظي (WebSocket)** في المرحلة 8.
3. «تعديل حركة منفّذة» مبني في الخدمات ومختبَر عبر العكس؛ نافذة تعديل مخصصة بالواجهة تُضاف مع مرحلة الصقل.
4. أرصدة المحل/الوسطاء بواجهة الكبير + الاعتماد/السحب UI → المرحلة 6 (الـ API جاهز).

---

## المرحلة 6 — الصناديق والكشوف والمطابقة (Boxes · Statements · Reconciliation) 📄

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07
- **الهدف المعلن:** واجهات الصناديق والتسويات + كشوف الحسابات + المطابقة بنقطة الإغلاق وإرسال الواتساب.

### ما أُنجز

**Backend:**
- **المطابقة (المشهد 4):** نموذج `Reconciliation` بلقطة أرصدة + خدمة تُرجع لكل عملة: رصيد سابق (من آخر مطابقة) + مدين/دائن **الفترة فقط** + الصافي. GET معاينة / POST تثبيت (نقطة إغلاق جديدة). **الكبير لأعضائه والصغير لنفسه** — والعزل مفحوص (403 لعضو مستأجر آخر).
- **قسم الحسابات (الجزء 10):** MembersViewSet للكبير — فتح (بفحص **حد الباقة**)، تعديل ملف العضو (هاتف/مجموعة واتساب اسم+رابط)، حظر/فك (يقطع الوصول فوراً)، وكل شيء في التدقيق.
- **كشوف:** كشف عضو للكبير + كشف ذاتي للصغير (لكل عملة).

**Frontend:**
- **الكبير `/office/members`:** جدول الأعضاء + فتح عضو (كل حقول المشهد 6) + حظر + **نافذة الحدود** (لكل عملة) + **نافذة المطابقة** (سابق/عليكم/لكم/الصافي + تثبيت + 📲 إرسال بقالب مركزي).
- **الكبير `/office/boxes`:** 3 تبويبات — صناديق الوسطاء (بطاقات بأرصدة كل عملة + إضافة صندوق باسم/رقم/عملات + **اعتماد/سحب** باسم عضو + كشف لكل عملة)، صندوق المحل (نقد كل عملة)، وإدارة العملات.
- **الصغير `/small/boxes`:** بطاقة لكل عملة (لك/عليك/الصافي) + كشف تفصيلي بمنظوره + **مطابقة: تثبيت وإرسال للواتساب بضغطة زر**.
- قالب `buildReconciliationMessage` مركزي في `lib/whatsapp.ts`.

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق1 المركزية | ✅ | قالب المطابقة مركزي؛ كل الشاشات من المكتبة؛ منطق المطابقة خدمة واحدة يستهلكها الدوران |
| ق2 العزل | ✅ | اختبار: الكبير 403 على مطابقة عضو مستأجر آخر؛ الأعضاء ضمن المستأجر حصراً |
| ق3 المحاسبة | ✅ | **ثابت المطابقة مختبَر:** السابق + مدين − دائن = رصيد الحساب الفعلي (تراكمي، لا تصفير)؛ نقطة الإغلاق تعرض حركات الفترة فقط (500 بعد 2000 مثبّتة) |
| ق4 RBAC | ✅ | members للكبير فقط؛ الصغير يطابق نفسه فقط |
| ق5 RTL | ✅ | 5 لقطات شاشة جديدة |
| ق7 الاختبارات | ✅ | **91 اختبار باكند** (+8: منطق المطابقة ونقطة الإغلاق والثابت، حد الباقة عند فتح عضو، حظر يقطع الوصول، كشوف الطرفين) + **6 فحوص E2E متصفح** (اعتماد 500→2,500، فتح عضو SML002 من الواجهة، تثبيت مطابقة والتحقق بإعادة الفتح، صناديق الصغير وكشفه) |
| ق8-ق9 | ✅ | lint/build نظيف؛ E2E على خوادم حقيقية |
| ق10 | ✅ | هذا القسم |

### ملاحظات وديون مؤجلة
1. استجابة POST المطابقة تعيد بيانات ما قبل التثبيت (الواجهة تتعامل معها بإعادة الفتح) — تُحسَّن استجابة الـ API في الصقل.
2. تصدير الكشوف PDF/Excel → المرحلة 7 (التقارير).
3. «استعادة كلمة مرور عضو» موجودة API (المرحلة 2) — زر لها في الواجهة يُضاف مع الصقل.

---

## المرحلة 7 — الأرباح والتقارير والتصدير (Reports & Export) 📊

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07
- **الهدف المعلن:** الـ13 تقريراً المعتمدة (الجزء 20) + فلتر تاريخ + تصدير PDF/Excel.

### ما أُنجز

**Backend (`apps/reports`):**
- **الـ13 بانياً** في `services.py` — كلٌّ يعيد `{title, columns, rows}` موحّدة: الملخص المالي لكل عملة، **الأرباح (فرق الأجور)** + حسب المكتب، حجم النشاط، ملخص المكاتب الصغيرة، ملخص الوسطاء، حركة صندوق المحل، حسب الحالة، حسب الوجهة، التسويات، أسعار الصرف (متوسط/أدنى/أعلى)، الأكثر نشاطاً، الكشف اليومي.
- **سجل مركزي** `REPORTS` يحدد أيها متاح للصغير (على بياناته حصراً) — الأرباح وملخصات الأعضاء/الوسطاء للكبير فقط.
- **نقطة موحّدة** `/api/{scope}/reports/?type=&date_from=&date_to=&export=` + نقطة الأنواع.
- **تصدير Excel** (openpyxl): ورقة RTL بترويسة بألوان الهوية وأعمدة تلقائية العرض.
- **تصدير PDF عربي** (reportlab + arabic-reshaper + bidi): جدول RTL بألوان الهوية — **بخط Amiri** بعد اكتشاف نقص أشكال الحروف في Tajawal (مصحَّح ومُتحقَّق بصرياً بتصيير الـPDF).

**Frontend:**
- مكوّن مركزي واحد `ReportsScreen` يخدم الدورين: اختيار النوع + نطاق تاريخ + جدول عام + **تنزيل فعلي** عبر `authedDownload` (blob بجلسة موثّقة — window.open لا يحمل التوكن).
- صفحتا `/office/reports` و`/small/reports` + بنود القوائم.

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق1 المركزية | ✅ | مكوّن شاشة واحد للدورين؛ بنية تقرير موحّدة تُغذّي العرض وExcel وPDF من نفس المصدر؛ سجل أنواع مركزي |
| ق2 العزل | ✅ | تقارير الصغير على بياناته فقط (مختبَر: activity=1 رغم وجود عضو آخر)؛ scope متقاطع 403 |
| ق3 المحاسبة | ✅ | رياضيات الربح مختبَرة: حركتان (15−10)+(10−7)=**8** ✓ بالاختبار الآلي، وE2E ببذرة 3 حركات (5+8+12)=**25** ظهر في الواجهة وفي التنزيلين |
| ق4 RBAC | ✅ | الأرباح محجوبة عن الصغير (403 + غير معروضة في قائمته، مختبَرة API وE2E) |
| ق5 RTL | ✅ | Excel بورقة RTL؛ PDF عربي متشكّل (لقطة `pdf-preview.png` بعد إصلاح الخط) |
| ق7 الاختبارات | ✅ | **101 اختبار باكند** (+10: ربح، فلتر تاريخ يستبعد، عدّ الحالات، نطاق الصغير، xlsx PK-header، PDF %PDF-header، نوع مجهول يسرد المتاح) + **E2E**: عرض الأرباح 25، تنزيل xlsx فعلي (5184 بايت) وPDF فعلي، تقارير أخرى تُعرض، وقائمة الصغير بلا أرباح |
| ق8-ق9 | ✅ | lint/build نظيف؛ E2E على خوادم حقيقية بتنزيلات حقيقية |
| ق10 | ✅ | هذا القسم + معاينة PDF |

### ملاحظات وديون مؤجلة
1. **اكتشاف موثّق:** خط Tajawal (نسخة google/fonts) ينقصه بعض أشكال العرض العربية — PDF انتقل لخط **Amiri** (كامل الأشكال). واجهة الويب تبقى Tajawal (سليمة هناك لأن المتصفح يشكّل بنفسه).
2. اسم معامل التصدير `export=` وليس `format=` (محجوز لدى DRF) — موثّق في الكود.
3. تصدير سجل الحركات نفسه (جدول الحركات الخام) يُغطّى عملياً بتقارير الحالة/الوجهة/النشاط؛ تصدير خام كامل يُضاف عند الحاجة.

---

## المرحلة 8 — الوقت الحقيقي والإشعارات (Real-time & Notifications) ⚡

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07
- **الهدف المعلن:** WebSockets + كل مُطلِقات الإشعارات (جرس + شارة) + البث + تحديث لحظي للقوائم.

### ما أُنجز

**Backend (`apps/notifications`):**
- **Notification**: 10 أنواع (حركة جديدة/قبول/رفض/قبض/عكس/تسوية/مطابقة/حد/اشتراك/تنبيه) + فهرس recipient×is_read.
- **خدمة `notify()` المركزية**: تحفظ + تدفع لحظياً لمجموعة المستخدم مع عدّاد غير المقروء؛ و`push_refresh(scope)` لأحداث تحديث البيانات.
- **WebSocket**: `NotificationsConsumer` + مصادقة توكن JWT بالاستعلام (مع فرض الحظر/الإيقاف) + توجيه ASGI؛ `daphne` أول التطبيقات ليخدم runserver عبر ASGI؛ **طبقة قنوات داخل الذاكرة تلقائياً بلا Redis** (والإنتاج/compose على Redis).
- **كل مُطلِقات الجزء 20-أ موصولة** في الأفعال: للصغير (قبول/رفض/قبض/عكس/تسوية/مطابقة/بث)، للكبير (حركة جديدة + refresh لحظي للجارية، تفعيل باقة، بث أدمن)، وللأدمن (طلب باقة).
- **بث الكبير → مكاتبه** (`OfficeBroadcast`): كان ديناً من المرحلة 3 — أُنجز الآن مع تفريع إشعار لكل عضو غير محظور.
- نقاط: قائمة الإشعارات + غير المقروء، قراءة الكل/واحد، بث الكبير + قراءة الصغير.

**Frontend:**
- `lib/ws.ts` مركزي: اتصال واحد، إعادة اتصال تلقائية، بثّ داخلي بحدث `hawalat:ws` موحّد.
- **NotificationBell** في ترويسة Shell (كل الأدوار): شارة رقمية حيّة + قائمة منسدلة + قراءة الكل.
- **تحديث لحظي**: الجارية للكبير تتحدّث عند وصول حركة؛ رئيسية الصغير وصناديقه عند تغيّر الأرصدة؛ تنبيهات الصغير عند البث.
- صفحتا التنبيهات: `/office/alerts` (بث لمكاتبي + الواردة من المنصة) و`/small/alerts`.

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق1 المركزية | ✅ | كل إشعار عبر `notify()` وحدها؛ عميل WS واحد بحدث موحّد؛ الجرس مكوّن واحد لكل الأدوار |
| ق2 العزل | ✅ | الإشعارات خاصة بالمستلم (مختبَر: إشعار الكبير لا يظهر للصغير)؛ مصادقة WS ترفض بلا توكن |
| ق4 RBAC | ✅ | بث الكبير لصغاره فقط؛ بث الأدمن للكبار فقط؛ نقاط البث محروسة بالأدوار |
| ق7 الاختبارات | ✅ | **110 اختبارات** (+9: كل المُطلِقات، تفريع البث لعضوين، غير المقروء/قراءة الكل، خصوصية، مستهلك WS: رفض بلا توكن + hello + **دفعة لحظية حقيقية عبر الطبقة**) |
| ق5-ق6-ق8 | ✅ | لقطات؛ لا ألوان خارج التوكِنز؛ lint/build نظيف |
| ق9 التشغيل | ✅ | **E2E بمتصفحين متزامنين على Daphne/ASGI حقيقي**: إرسال الصغير → جارية الكبير تحدّثت **بلا إعادة تحميل** + شارة 1 حيّة؛ القبول → جرس الصغير lively؛ البث → وصل لحظياً (شارة 2) |
| ق10 | ✅ | هذا القسم + لقطتا `live-pending.png` و`live-bell.png` |

### ملاحظات وديون مؤجلة
1. إشعار «اقتراب الحد السالب» النوع جاهز (LIMIT) — ربط عتبة الـ80% عند القبول يُضاف في الصقل.
2. انتهاء التوكن أثناء جلسة WS طويلة: يعاد الاتصال تلقائياً لكن بتوكن الجلسة القديم — تحديث التوكن عبر refresh قبل إعادة الاتصال يُضاف في مرحلة الأمان.
3. صوت الإشعار غير مضمّن (اخترنا جرس+شارة كما حُسم) — يسهل إضافته لاحقاً.

---

## المرحلة 9 — تكامل الواتساب (WhatsApp Integration) 🟢

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07
- **الهدف المعلن (الجزء 17 §6):** رابط افتراضياً + بوت اختياري بمفتاح: مفعّل → تلقائي؛ مطفأ → يدوي.

### ما أُنجز

**Backend (`apps/whatsapp`):**
- `WhatsAppSettings` لكل مستأجر: **مفتاح البوت** + عنوان بوابة + رمز — يتحكم بها الكبير حصراً.
- **Outbox** (`WhatsAppMessage`): كل رسالة بحالة (قيد/أُرسلت/فشلت) + محاولات + آخر خطأ.
- **محوّل بوابة عام**: POST `{chatId, text}` برأس Bearer — متوافق مع بوابات ذاتية (WAHA-style)؛ المزوّد يُضبط من الشاشة بلا كود.
- **مهمة Celery** بإعادة محاولة (حتى 3) — وEager تلقائياً محلياً بلا Redis.
- خدمة `queue_message`: تقرر الوضع — بوت جاهز + معرّف مجموعة → طابور؛ وإلا None (يدوي).
- نقاط: حالة البوت للمستخدم، إرسال (الصغير لمجموعته حصراً/الكبير لأعضائه حصراً)، إعدادات الكبير، سجل الصادر.
- **ربط تلقائي**: إنشاء حركة والبوت مفعّل → رسالة الحركة تُرسل فوراً لمجموعة المُنشئ.
- حقل `whatsapp_chat_id` في ملف العضو (اختياري — يلزم للبوت فقط).

**Frontend:**
- صفحة `/office/settings`: مفتاح البوت + البوابة + شارة جاهزية + **سجل رسائل البوت**.
- **زر الإرسال الذكي** في إرسال الحركة: بوت جاهز → «🤖 إرسال عبر البوت» (فوري) مع **تحوّل تلقائي للرابط اليدوي عند فشل البوت**؛ وإلا الزر اليدوي المعتاد.
- حقل معرّف مجموعة البوت في إنشاء العضو.

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق1 المركزية | ✅ | قرار الوضع في `queue_message` وحدها؛ المحوّل عام؛ النصوص من قوالب `lib/whatsapp` المركزية |
| ق2 العزل | ✅ | الصغير لمجموعته فقط؛ الكبير مُنع (400) من الإرسال لعضو مستأجر آخر (مختبَر) |
| ق4 RBAC | ✅ | الإعدادات والصادر للكبير حصراً (الصغير 403، مختبَر) |
| ق7 الاختبارات | ✅ | **119 اختباراً** (+9: المفتاح، 409 يدوي، إرسال ببوابة موهومة مع فحص الترويسة والحمولة، فشل→failed بعد محاولات، الربط التلقائي عند الإنشاء، لا طابور والبوت مطفأ) |
| ق8 | ✅ | lint/build نظيف |
| ق9 التشغيل | ✅ | **E2E ببوابة HTTP مزيفة حقيقية**: الوضع اليدوي (زر الرابط) → تفعيل البوت من الواجهة → الزر تحوّل لـ«عبر البوت» → **البوابة استقبلت رسالتين فعلياً** (التلقائية عند الإنشاء + اليدوية) بالنص والمجموعة الصحيحين |
| ق10 | ✅ | هذا القسم + لقطتا الإعدادات والإرسال عبر البوت |

### ملاحظات وديون مؤجلة
1. المزوّد الحقيقي (WAHA/بديل) يُوصَل عند النشر بإدخال عنوانه ورمزه فقط — لا كود.
   **توضيح معتمد من المالك:** ربط حساب الواتساب بالبوابة يتم **بمسح باركود QR**
   (كواتساب ويب) مرة واحدة عند الإعداد — يحدث في البوابة. **تحسين معتمد لمرحلة
   النشر:** عرض الباركود داخل شاشة إعداداتنا مباشرة عبر API البوابة، فيتم الربط
   كاملاً من داخل النظام (افتح الإعدادات → امسح → جاهز).
2. زر «اختبار الاتصال» في الإعدادات يُضاف في الصقل (السجل يكشف الفشل حالياً).
3. رسالة المطابقة عبر البوت: زر المطابقة يستخدم القالب المركزي — تحويل زرَي المطابقة للوضع الذكي (مثل زر الحركة) في الصقل.

---

## المرحلة 10 — PWA والصقل (PWA & Polish) 📱

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07
- **الهدف المعلن:** التثبيت كتطبيق (SW + قشرة أوفلاين) + سداد الديون الصغيرة المؤجلة.

### ما أُنجز

**PWA:**
- **Service Worker** (`public/sw.js`): قشرة أوفلاين (الجذر/الدخول/manifest/الأيقونة)، أصول Next الساكنة cache-first، التنقّل network-first مع الرجوع للمخزّن ثم القشرة — **و`/api/` و`/ws/` لا تُلمس أبداً** (بيانات مالية حيّة).
- تسجيل تلقائي (`PwaSetup` في الجذر) + **زر «📲 تثبيت التطبيق»** في Shell يظهر عندما يسمح المتصفح (beforeinstallprompt).

**سداد الديون المؤجلة:**
- **تعديل حركة مقبولة من الواجهة** (دين م5): نقطة `/edit/` فوق خدمة العكس+إعادة الترحيل، بإشعار الصغير وتحديث رصيده لحظياً، ونافذة تعديل في السجل (للمقبولة غير المدفوعة).
- **تنبيه اقتراب الحد 80%** (دين م8): بعد كل قبول يُفحص رصيد العضو مقابل حدّه، وينبَّه الكبير عند ≥80%.
- **استعادة كلمة مرور عضو من الواجهة** (دين م6): زر 🔑 في صف العضو → نافذة → API الاستعادة الهرمية.
- **مطابقة ذكية بالبوت** (دين م9): زرا المطابقة (الكبير لعضوه والصغير لنفسه) يحاولان البوت أولاً ثم يتحولان تلقائياً للرابط اليدوي.

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق1 المركزية | ✅ | الصقل كله فوق الخدمات/القوالب المركزية القائمة — صفر منطق مكرر |
| ق3 المحاسبة | ✅ | اختبار التعديل: المستحقة 15→25 → رصيد العضو 1025 بعد العكس وإعادة الترحيل |
| ق4 RBAC | ✅ | الصغير ممنوع من `/edit/` (403، مختبَر) |
| ق7 الاختبارات | ✅ | **123 اختباراً** (+4: التعديل يعكس ويعيد، إشعار التعديل، منع الصغير، تنبيه الـ80%) |
| ق8-ق9 | ✅ | lint/build نظيف؛ **E2E**: SW نشط والقشرة مخزّنة (`hawalat-shell-v1`) + تعديل من الواجهة ظهر في الجدول + استعادة كلمة مرور ثم **دخول فعلي بالجديدة** |
| ق10 | ✅ | هذا القسم + لقطة نافذة التعديل |

### ملاحظات وديون مؤجلة
1. زر التثبيت يعتمد `beforeinstallprompt` (كروم/أندرويد) — على iOS التثبيت من قائمة المشاركة (سلوك النظام).
2. تعديل حركة **مدفوعة** يتطلب عكس القبض أولاً (رسالة توضيحية في النافذة) — سلوك محاسبي مقصود.
3. بقية ديون WS token refresh → المرحلة 11 كما هو مخطط.

---

## المرحلة 11 — الأمان والاختبار (Security & QA) 🛡️

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07
- **الهدف المعلن:** إبطال الجلسات + تحديد المعدّل + ترويسات + تجديد التوكن + مسح أمني شامل.

### ما أُنجز

**تقوية المصادقة:**
- **إبطال refresh نهائياً عند الخروج** (`token_blacklist` + نقطة `/api/auth/logout/`) — مختبَر: التجديد بعد الخروج 401.
- **تدوير refresh** مع إبطال القديم بعد كل تجديد (مختبَر: القديم يُرفض).
- **محقّقات كلمات المرور** (طول 8 + شائعة + رقمية + تشابه السمات) — كانت ناقصة من تقسيم الإعدادات.
- ترويسات: `X-Frame-Options: DENY` + `Referrer-Policy: same-origin` (فوق تشديدات prod القائمة).

**تحديد المعدّل (Throttling):**
- الدخول 10/دقيقة والتسجيل 5/دقيقة للمجهول، والموثّق 1000/دقيقة — معطّل في بيئة الاختبار ومُختبَر صراحةً (3 محاولات → 429).

**تجديد التوكن التلقائي (Frontend):**
- `authedApi`: عند 401 → تجديد → إعادة محاولة واحدة → وإلا خروج نظيف.
- **WS**: تجديد التوكن قبل كل إعادة اتصال (دين م8 مسدَّد).
- زر الخروج يستدعي `/logout/` فيُبطل refresh على الخادم قبل مسح الجلسة.

**المسح الأمني الشامل (`test_security.py`):**
- **مسح المجهول**: 11 نقطة محمية كلها 401.
- **مسح الأدوار**: الصغير 403 على 11 نقطة كبير؛ **الأدمن 403 على كل النقاط المالية** (إداري بحت — مثبت آلياً).
- **الثابت المحاسبي الكلي**: بعد اعتماد+حركة+قبض+سحب+عكس → مجموع مدين النظام = مجموع دائنه.
- **اكتمال التدقيق**: أفعال (حد/اعتماد/إنشاء/حظر) كلها تترك أثراً.

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق2 العزل | ✅ | مسوح الأدوار الشاملة + كل اختبارات العزل السابقة ما زالت خضراء |
| ق3 المحاسبة | ✅ | الثابت الكلي مدين=دائن على مستوى النظام بعد عمليات مختلطة |
| ق4 RBAC | ✅ | ثلاثة مسوح آلية (مجهول/صغير/أدمن) على قوائم نقاط ممثِّلة |
| ق7 الاختبارات | ✅ | **131 اختباراً** (+8 أمنية) كلها تمرّ |
| ق8 | ✅ | ruff + eslint + build نظيفة |
| ق10 | ✅ | هذا القسم |

### ملاحظات وديون مؤجلة
1. Throttle عبر ذاكرة التخزين المحلية (LocMem) — في الإنتاج متعدد العمليات يُستخدم Redis cache backend (يُضبط في مرحلة النشر).
2. تنظيف دوري لجدول التوكنات المبطَلة (`flushexpiredtokens`) — يُجدول مع مهام النشر.
3. مراجعة اختراق يدوية خارجية تبقى توصية قبل الإطلاق التجاري.

---

## المرحلة 12 — النشر والتشغيل (Deploy & Ops) 🚀

- **الحالة:** ✅ **مقبولة ومغلقة** — 2026-08-07 — **وبها تكتمل الخطة (13/13)** 🎉
- **الهدف المعلن:** Docker إنتاجي + CI/CD + مهام دورية + نسخ احتياطي + الشروط + دليل نشر.

### ما أُنجز

**بنية الإنتاج:**
- `Dockerfile.prod` للباكند (prod settings + collectstatic + **Daphne** يخدم HTTP+WS) وللواجهة (**multi-stage standalone** خفيف).
- `docker-compose.prod.yml`: db + redis + backend + worker + **beat** + frontend — restart دائم، منافذ على 127.0.0.1 خلف بروكسي، ترحيلات تلقائية، ومتغيرات إلزامية تفشل مبكراً إن نُسيت (كلمة قاعدة قوية، عناوين عامة). **مُتحقق بنيوياً**.
- كاش **Redis** في الإنتاج (تحديد المعدّل يعمل عبر كل العمليات — دين م11 مسدَّد).

**المهام الدورية (beat):**
- `expire_subscriptions` يومياً: تعليم المنتهية + إشعار صاحبها (مختبَرة: انتهاء + إشعار + عدم تكرار).
- `flush_expired_tokens` يومياً: تنظيف جدول الإبطال (دين م11 مسدَّد).

**الشروط وسياسة الاستخدام:**
- صياغة احترافية من 8 أقسام (تعريفات، طبيعة الخدمة «توثيق محاسبي — ليست بنكاً»، الهرمية، لا-حذف، الخصوصية والعزل، الاشتراكات والترحيل، المسؤولية، أحكام) مع تنويه المراجعة القانونية — صفحة `/terms` مربوطة من الدخول.

**التشغيل:**
- أمر `createplatformadmin` (بمحقّقات كلمة المرور) لإنشاء أدمن المنصة.
- **دليل نشر كامل** `docs/06-deployment.md`: خادم، env إنتاجي، تشغيل، بروكسي TLS (مثال Caddy مع WS)، **إعداد بوت الواتساب بمسح QR (كما وضّح المالك)**، نسخ احتياطي cron مع الاستعادة، تحديثات بلا توقف، وقائمة فحص بعد النشر، واستكشاف أخطاء.
- README محدّث لحالة الاكتمال.

### فحص القبول

| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق7 الاختبارات | ✅ | **133 اختباراً** كلها تمرّ (+2 للمهام الدورية) |
| ق8 الجودة | ✅ | ruff + format + eslint نظيفة |
| ق9 التشغيل | ✅ | compose التطوير والإنتاج كلاهما صالح بنيوياً؛ الإنتاج يرفض الإقلاع بدون أسرار |
| ق10 التوثيق | ✅ | دليل نشر كامل + شروط + README ختامي |
| بقية القواعد | ✅ | لا تغيير على المنطق — كل اختبارات المراحل السابقة خضراء |

### ملاحظات التسليم
1. **الفحص الحي النهائي على خادم حقيقي** (compose up فعلي + TLS + بوابة واتساب حقيقية) يتم عند النشر الأول بيد المالك متبعاً الدليل — بيئة العمل السحابية بلا Docker daemon.
2. الشروط تُراجع قانونياً قبل الإطلاق التجاري (منصوص في الصفحة ذاتها).
3. توصية مستقبلية: مراقبة (Sentry/Uptime) وضغط اختبار حمل عند نمو الاستخدام.

---

# 🏁 الخطة مكتملة: 13/13 مرحلة — كلها ✅ مقبولة ومغلقة بالفحص الموثّق

---

# 🔧 سجل التجربة الحيّة (بعد النشر التجريبي على Render)

> دورة العمل المتفق عليها: المستخدم يجرّب النسخة الحيّة ويكتب الملاحظة → نصلحها فوراً → دفع → نشر تلقائي (3–7 دقائق).

## ملاحظة 1 — استبدال الإيموجي بمكتبة أيقونات (2026-08-08)

- **الملاحظة:** «مانك مثبّت مكتبة أيقونات ومستخدم إيموجي بدل الأيقونات + حط أيقونات معبّرة لكل زر وعنصر».
- **الإصلاح:**
  - تثبيت **lucide-react** (مكتبة الأيقونات الرسمية المتوافقة مع Tailwind).
  - `Shell.tsx`: عنصر التنقّل أصبح يستقبل `icon: LucideIcon` مركزياً — كل القوائم الجانبية (أدمن/كبير/صغير) بأيقونات.
  - أيقونات معبّرة لكل زر: قبول ✓/رفض ✗/معالجة/مدفوعة/تسليم/تعديل/عكس/اعتماد/سحب/مطابقة/كشف/حفظ/دخول/خروج/بث/تقارير (Excel/PDF/عرض)… إلخ.
  - إزالة كل الإيموجي من الواجهة (جرس، تثبيت PWA، بث، مفاتيح، نجاح الإرسال…) — فحص شامل بلا نتائج.
- **الفحص:** `eslint` نظيف + `next build` ناجح + بحث إيموجي شامل = صفر نتائج. (ق1 المركزية: الأيقونة جزء من تعريف `NavItem` المركزي وحجم موحّد `size-4/size-5`).

## ملاحظة 2 — العملات الافتراضية + الهيكل العائم + فورم دخول احترافي (2026-08-08)

- **الملاحظة:** «لم تُضف العملات المتفق عليها (تركي/يورو/سوري/دولار) + سايدبار وتوب بار احترافيان بحواف دائرية عائمة ببادينغ خفيف + فورم تسجيل احترافي بأيقونات وعين لإظهار/إخفاء كلمة المرور بكل الحقول».
- **الإصلاح:**
  1. **العملات:** `seed_default_currencies` في `boxes/services.py` تزرع USD/EUR/TRY/SYP تلقائياً عند فتح أي مكتب كبير (تُستدعى من `create_big_office`)، + هجرة بيانات `0003` تزرعها للمستأجرين الحاليين على النسخة الحيّة. المكتب يبقى حراً بالإضافة/التعطيل.
  2. **الهيكل العائم:** `Shell.tsx` — سايدبار وتوب بار عائمان `rounded-2xl` بظل خفيف وبادينغ حول الصفحة، العنصر النشط بتعبئة كاملة بلون الهوية، التوب بار لاصق مع blur.
  3. **حقول كلمات المرور:** مكوّن مركزي جديد `PasswordInput` (قفل + عين إظهار/إخفاء) ودعم `icon/trailing` في `Input` المركزي — طُبّق على: الدخول، فتح مكتب كبير (أدمن)، فتح عضو واستعادة كلمة مروره (الكبير)، رمز بوابة البوت. فورم الدخول بأيقونة مستخدم و placeholder.
- **الفحص:** 135 اختبار باكند ✅ (منها اختبارا زرع العملات وعزلها) + `eslint` + `next build` ✅ + لقطات شاشة حيّة (دخول بعين مفعّلة، اللوحة العائمة، الصناديق). (ق1: العين والأيقونات عبر مكوّن مركزي واحد لا تكراراً في الصفحات).

## ملاحظة 3 — اسم المرسِل اختياري + زر الإرسال محمي (2026-08-08)

- **الملاحظة:** «اسم المرسل اختياري ولكن الباقي إجباري — زر الحركة يجب أن يكون محمياً يتفعل بعد اكتمال البيانات».
- **الإصلاح:**
  - **باكند:** `sender` صار `blank=True, default=""` (+ هجرة) والسيريالايزر `required=False, allow_blank=True` — بقية الحقول تبقى إجبارية بـ400. نص إشعار/بوت الحركة يتخطى سطر المرسِل إن كان فارغاً.
  - **واجهة:** حقل «اسم المرسِل (اختياري)» بلا required، وزر «إرسال الحركة» معطّل (`disabled`) حتى اكتمال: المستفيد + مبلغ موجب + العملة المقبوضة + الوجهة. قالب رسالة الواتساب يتخطى سطر المرسِل الفارغ.
- **الفحص:** اختبار جديد يثبت 201 بلا مرسِل و400 عند غياب أي حقل إجباري — 136 اختباراً ✅ + بناء الواجهة و lint ✅.

## ملاحظة 4 — ثيم داكن بزر تبديل + أزرار مميزة + إعدادات المكتب الصغير (2026-08-08)

- **الملاحظة:** «لا أرى أيقونة تبديل الثيم + اجعل تثبيت التطبيق زراً منفصلاً بلون مميز وتسجيل الخروج بأحمر واضح + لا يوجد قسم إعدادات بالمكاتب الصغيرة (اسم وكود للقراءة، تغيير كلمة مرور، صورة بروفايل تظهر بالتوب بار)».
- **الإصلاح:**
  1. **الثيم:** زر تبديل (قمر/شمس) في التوب بار — `ThemeToggle` يضبط `data-theme` ويحفظ الاختيار، سكربت إقلاع في layout يطبّقه قبل الرسم (بلا وميض)، وطبقة التوكِنز أُعيدت هيكلتها: النظام افتراضياً واختيار المستخدم يغلبه (`:root[data-theme="dark"]` + حارس `:not([data-theme="light"])`) مع `color-scheme`.
  2. **الأزرار:** «تثبيت التطبيق» زر ذهبي (accent) منفصل تماماً، «تسجيل الخروج» أحمر ممتلئ واضح (+ وضع مضغوط دائري للتوب بار على الجوال).
  3. **إعدادات الصغير:** صفحة `/small/settings` جديدة — معلومات المكتب للقراءة فقط (الاسم/الكود/المستخدم/الكبير التابع له)، تغيير كلمة مرور (حالية + جديدة + تأكيد بعين)، وصورة بروفايل تُقص مربعة وتُصغّر 256px وتُخزن data URL (تعمل على أي استضافة بلا تخزين ملفات) وتظهر فوراً بالتوب بار (`UserBadge` + حدث `hawalat:session`). باكند: حقل `avatar` + `PATCH /api/auth/me/` بحد حجم وتحقق صيغة.
  4. **إصلاح جانبي:** تفعيل التنقّل صار «أطول مسار مطابق فقط» — كانت «الرئيسية» تضيء مع كل قسم فرعي.
- **الفحص:** 138 اختباراً ✅ (منها اختبارا الصورة: تحديث/قراءة + رفض الصيغ الخاطئة والأحجام الكبيرة) + بناء و lint ✅ + لقطات حيّة بالوضعين الفاتح/الداكن مع ثبات الثيم بعد إعادة التحميل ورفع صورة فعلي ظهر بالتوب بار.

## ملاحظة 5 — كروت حالات ذكية في سجل الحركات (2026-08-08)

- **الملاحظة:** «سجل الحركات: ساوي كروت ذكية إحصائيات وبنفس الوقت تعمل فلتر بالحالات + زر الرئيسية دائماً نشيط».
- **الإصلاح:**
  - مكوّن مركزي جديد `StatusFilterCards`: صف كروت (أيقونة ملوّنة + عدد + اسم الحالة) — الضغط يفلتر الجدول ويُبرز الكرت، وضغطة ثانية تعيد «الكل». طُبّق على سجل الصغير (الكل/قيد الانتظار/مقبولة/مدفوعة/تم التسليم/مرفوضة/معكوسة) وسجل الكبير (نفسها بلا «قيد الانتظار»). الفلترة محلية حتى تبقى الأرقام شاملة، وحُذفت قائمة «الحالة» المنسدلة القديمة.
  - توحيد مصطلح مركزي: شارة الحالة `cancelled` صارت «مرفوضة» بدل «ملغية» (لغة السرد).
  - «الرئيسية دائماً نشطة»: أُصلحت في الدفعة السابقة (أطول مسار مطابق فقط) — التجربة كانت قبل اكتمال نشرها؛ أُكّدت بالفحص الحي: النشط «سجل الحركات» وحدها.
- **الفحص:** بناء و lint ✅ + لقطات حيّة: 7 كروت بعدادات صحيحة، الضغط على «مقبولة» فلتر الجدول لسجلّين وأبرز الكرت. (ق1: الكروت مكوّن مركزي واحد generic للدورين).

## ملاحظة 6 — قائمة الإشعارات مقصوصة على الجوال + زر إرسال زائد بالرئيسية (2026-08-08)

- **الملاحظة:** «شوف الإشعارات كيف» (لقطة جوال: اللوح ضيق ونصوصه مبتورة ويخرج عن الشاشة) + «إرسال حركة بالرئيسية لا داعي له».
- **الإصلاح:**
  - **الإشعارات:** على الجوال صار اللوح `fixed` بعرض الشاشة تقريباً (لا يُقص أبداً)، وعلى الشاشات الأوسع منسدل من الجرس يفتح للداخل (`end-0`) بعرض أكبر (24rem). النصوص تلتف (`break-words`) بدل البتر، والتاريخ `dir=ltr` فلا تتكسر أرقامه، وارتفاع التمرير `60vh` على الجوال.
  - **الرئيسية:** حُذف زر «إرسال حركة» (المكرر — موجود بالقائمة الجانبية) من ترويسة «آخر الحركات» ومن حالة الفراغ.
- **الفحص:** بناء و lint ✅ + لقطات حيّة بعرض 390px (جوال) و1440px: اللوح كامل الظهور والنصوص كاملة بالوضعين.

## ملاحظة 7 — إشعار التسليم للصغير + لا عكس بعد التسليم (2026-08-08)

- **الملاحظة:** «في حال تم التسليم المكتب الصغير لازم يعرف أن حركة زبونه تسلمت + بعد التسليم ما يصير تنعكس — الزبون ممكن يلغي قبل ما يستلم، مستحيل بعد ما يستلم».
- **الإصلاح:**
  - **إشعار تسليم:** نوع إشعار جديد `txn_delivered` — عند تعليم «تم التسليم» يصل الصغير إشعار «سُلّمت حركتك …» باسم المستفيد والوجهة (لحظي عبر الجرس).
  - **قفل التسليم:** `reverse_transaction` و`edit_accepted_transaction` يرفضان (400) أي حركة مسلَّمة. المخرج الوحيد لخطأ التعليم: زر «تراجع عن التسليم» (مدقَّق في سجل التدقيق) ثم العكس.
  - **الواجهة:** بعد التسليم يختفي «عكس» و«تعديل» من سجل الكبير ويظهر «تراجع عن التسليم» فقط.
  - وثّقت القاعدة في الدورة التشغيلية §4-ج.
- **الفحص:** 4 اختبارات جديدة (الإشعار يصل + العكس والتعديل مرفوضان بعد التسليم + العكس يعود متاحاً بعد التراجع) — 143 اختباراً ✅ + بناء و lint ✅.

## ملاحظة 8 — أرقام نظيفة + كرت عملة ذكي بكشف «لنا/لكم» (2026-08-08)

- **الملاحظة:** «الأصفار الزائدة ليس لها معنى + أسهم زيادة المبلغ ليس لها معنى احذفها + ألغِ زر الكشف من الكرت وخليه كرت ذكي يضغط عليه تظهر التفاصيل بالأسفل جدول لنا ولكم».
- **الإصلاح:**
  - **`formatMoney` المركزي:** كسور فقط عند الحاجة (`1,000` بدل `1,000.00` و`10.5` تبقى) — انعكس على كل شاشة تلقائياً (ق1).
  - **أسهم حقول الأرقام:** أُخفيت مركزياً في globals.css (`appearance: textfield` + إخفاء spin buttons) — كل حقول المبالغ دفعة واحدة.
  - **صناديق الصغير:** حُذف زر «الكشف»؛ الكرت نفسه صار ذكياً (hover + سهم يدور + إطار عند التفعيل) — الضغط يفتح **كشفاً تفصيلياً أسفل الكروت** بدل النافذة: 3 مجاميع (لنا/لكم/الصافي) + جدول بأعمدة «لنا» و«لكم» لكل قيد بوقته، وضغطة ثانية تغلقه.
- **الفحص:** بناء و lint ✅ + لقطة حيّة بقيود فعلية: اعتماد 2000 (لنا) + قبول 1015 (لكم) = صافٍ 985 لنا — الأرقام كلها بلا أصفار زائدة.

## ملاحظة 9 — التسليم نهائي مطلق: لا «تراجع عن التسليم» (2026-08-08)

- **الملاحظة (تصحيح لملاحظة 7):** «ما في شي اسمه تراجع عن التسليم — مجرد ما أصبح تم التسليم انتهى الأمر، لا يمكن إجراء أي إجراء كان، لأن الزبون استلم وذهب».
- **الإصلاح:**
  - حُذف مخرج «التراجع عن التسليم» نهائياً — `mark_delivered` صار باتجاه واحد: يرفض أي استدعاء لحركة مُسلَّمة (حتى إعادة التعليم)، ويرفض التسليم لغير المقبولة.
  - رسائل العكس/التعديل صارت: «التسليم نهائي» بلا أي بديل مقترح.
  - الواجهة: الحركة المسلَّمة تعرض «سُلّمت — نهائي» بلا أي زر إجراء.
  - حُدّثت الدورة التشغيلية §4-ج: التسليم نهائي مطلق وتعليمه مسؤولية.
- **الفحص:** اختباران محدّثان (كل محاولات ما بعد التسليم = 400 والحالة ثابتة + لا تسليم لغير المقبولة) — 144 اختباراً ✅ + بناء و lint ✅.

## ملاحظة 10 — تثبيت المطابقة بإذن الكبير + ملاحظات توضيحية (2026-08-08)

- **الملاحظة:** «المكتب الصغير لا يرسل مطابقة إلا إذا سمح المكتب الكبير بذلك من الإعدادات + نضيف ملاحظة بالأماكن التي تحتاج ملاحظة».
- **الإصلاح:**
  - حقل `allow_small_reconciliation` على المستأجر (افتراضياً **ممنوع**) + نقطة `GET/PATCH /api/office/preferences/` للكبير حصراً (مدقَّقة)، وبطاقة «صلاحيات المكاتب الصغيرة» في إعدادات الكبير بمفتاح تبديل وشرح.
  - مطابقة الصغير: **المعاينة متاحة دائماً** — أما «تثبيت وإرسال» فيظهر فقط عند السماح، والخادم يرد 403 لغير المسموح. الكبير يثبّت مطابقات أعضائه دائماً.
  - **ملاحظات توضيحية** حيث يلزم: إعدادات الكبير (لماذا يُنصح بإبقاء المطابقة بيده)، نافذة مطابقة الصغير (معاينة فقط + كيف تتفعل)، سجل الكبير (التسليم نهائي + معنى مدفوعة)، فورم الإرسال (لا تعديل بعد الإرسال — التصحيح عبر مكتبك).
- **الفحص:** 3 اختبارات جديدة (403 افتراضياً ثم 201 بعد التفعيل + النقطة للكبير حصراً + الكبير يثبّت دائماً) — 146 اختباراً ✅ + بناء و lint ✅.

## ملاحظة 11 — إلغاء قسم التقارير للمكتب الصغير (2026-08-08)

- **الملاحظة:** «المكتب الصغير لا يحتاج قسم تقارير أساساً — هو فقط يرسل الحركات ويدير عملياته بصناديقه، يدير نفسه، لا حاجة للتقارير أصلاً».
- **الإصلاح:**
  - حُذف بند «التقارير» من قائمة الصغير وصفحة `/small/reports` كاملة.
  - أُزيلت مسارات `/api/small/reports/` من الخادم نهائياً (404) — تقارير المكتب الكبير كما هي.
  - حُدّث دفتر الاكتشاف (قائمة أقسام الصغير) بالتصحيح، وبقي لديه ما يغنيه: كروت صناديقه بالكشف التفصيلي «لنا/لكم» والمطابقة وسجل حركاته بالكروت الذكية.
- **الفحص:** اختبارات محدّثة (المساران 404 + تقارير الكبير 403 على الصغير) — 143 اختباراً ✅ + بناء و lint ✅.

## ملاحظة 12 — قسم «المطابقات» بسجل دائم للصغير (2026-08-08)

- **الملاحظة:** «يجب أن يكون لديه قسم المطابقات — كل مطابقة لها سجل، إذا حصل أي خطأ يستطيع العودة إليها فيرى أين حصل الخطأ».
- **الإصلاح:**
  - **لقطة أغنى:** `commit_reconciliation` صار يخزّن التفصيل الكامل لكل عملة (سابق/مدين/دائن/رصيد) لا الرصيد النهائي فقط — حتى يُعرف أين حصل الفرق فعلاً.
  - **نقاط جديدة:** `GET /api/small/reconciliations/` (سجل الصغير لنفسه) و`GET /api/office/members/<id>/reconciliations/` (الكبير لعضوه) — عزل كامل، الغريب 403.
  - **قسم «المطابقات»** في قائمة الصغير: زر «مطابقة جديدة» (المعاينة/التثبيت انتقلا هنا من الصناديق) + سجل كل المطابقات بالأحدث أولاً — كل سجل كرت قابل للفتح يعرض لقطة التثبيت كاملة (الرصيد السابق/عليك/لك/الصافي المثبّت) بصيغة لك/عليك الواضحة، مع من ثبّتها ومتى، وملاحظة أن اللقطة لا تتغير أبداً.
- **الفحص:** اختبار جديد شامل (لقطتان متتاليتان بقيمهما الدقيقة + عزل) — 144 اختباراً ✅ + بناء و lint ✅ + لقطة حيّة: سجلّان 985 لك ثم 1,335 لك بعد اعتماد 350، والتفصيل مطابق.

## ملاحظة 13 — اسم المكتب ديناميكياً + البريد الإلكتروني (2026-08-08)

- **الملاحظة:** «زر مطابقة الصناديق: الغه (كان أُلغي بالدفعة السابقة ✓) + "مكتبك الكبير" نخليها "مكتب" والاسم ديناميكي + "المكتب الكبير التابع له" → "المكتب التابع له" + نضيف البريد الإلكتروني وبفورم التسجيل إلزامي لأننا سنعتمد عليه باستعادة كلمات المرور».
- **الإصلاح:**
  - **النصوص:** كل «مكتبك الكبير» صارت «مكتب {الاسم}» ديناميكياً من الجلسة (فورم الإرسال، المطابقات، إعدادات الصغير)، والحقل صار «المكتب التابع له».
  - **البريد الإلكتروني:** ظاهر ومحرَّر في إعدادات الصغير (بطاقة مستقلة بتلميح «سيُعتمد عليه لاستعادة كلمة المرور»)، `PATCH /me/` يقبله بتحقق صيغة وفرادة، وحقل بريد في فورمي فتح مكتب كبير (أدمن) وفتح عضو (الكبير)، و**إلزامي في التسجيل الذاتي** مع فرادة.
- **الفحص:** 3 اختبارات جديدة (التسجيل يرفض بلا بريد/بصيغة خاطئة + me يعرض ويحدّث + رفض المكرر وغير الصالح) — 147 اختباراً ✅ + بناء و lint ✅.

## ملاحظة 14 — الصغير يشاهد المطابقة وينزّلها PDF فقط + تلوين الصناديق (2026-08-08)

- **الملاحظة (تنقّح ملاحظة 10):** «لا يوجد زر مطابقة لدى المكتب الصغير — فقط مشاهدة المطابقة وتنزيلها PDF + الصناديق: سالب = حمراء (مطلوب منك المبلغ)، خضراء = لك».
- **الإصلاح:**
  - **المطابقة للعرض فقط:** حُذف زر «مطابقة جديدة» ونافذته من قسم مطابقات الصغير — التثبيت من المكتب الكبير حصراً، والخادم يرفض POST من الصغير دائماً (403). أُلغيت صلاحية `allow_small_reconciliation` وبطاقتها من إعدادات الكبير ونقطة التفضيلات (لم يعد لها معنى) + هجرة حذف الحقل.
  - **تنزيل PDF:** نقطة `GET .../reconciliations/<id>/pdf/` (للصغير عن نفسه وللكبير عن عضوه) تولّد PDF عربياً من اللقطة المثبّتة (بنية تصدير التقارير نفسها — خط Amiri) وزر «تنزيل PDF» في لوح تفاصيل كل سجل.
  - **تلوين الصناديق:** كرت العملة يصبح بإطار وخلفية **حمراء** عند الصافي السالب (مطلوب منك) و**خضراء** عند الموجب (لك).
- **الفحص:** اختبار معاد كتابته (الصغير: معاينة 200 + تثبيت 403 دائماً + PDF فعلي يبدأ بـ %PDF + لا وصول لسجل غيره 404) — 147 اختباراً ✅ + بناء و lint ✅ + لقطات حيّة.

## ملاحظة 15 — كود بالتوب بار + لوغو المنصة + تصدير السجل + كروت الجوال (2026-08-08)

- **الملاحظة:** «كود المكتب للتوب بار بجانب الاسم + مكانه لوغو المنصة يرفعه الأدمن + سجل الحركات: PDF وExcel حسب الكرت المختار + الغِ BIG001- من المرجع + نظام كروت/جداول بزر ذكي (كروت على الجوال بلا سحب أفقي) + احذف تلميح البريد».
- **الإصلاح:**
  1. **الكود بالتوب بار:** شارة `OfficeCodeChip` بجانب اسم المكتب — واختفى من الشعار الجانبي.
  2. **لوغو المنصة:** حقل `logo` في إعدادات المنصة + بطاقة رفع في لوحة الأدمن (تصغير تلقائي بحفظ الشفافية) + نقطة عامة `/api/platform/branding/` + مكوّن `PlatformLogo` مركزي في الشعار الجانبي وشاشة الدخول (الافتراضي «ح» إن لم يُرفع).
  3. **تصدير السجل:** `export=xlsx|pdf` على سجلي الصغير والكبير — يحترم البحث **والكرت المختار** (الكل/مرفوضة/مدفوعة…) ويطبع اسم الفلتر في عنوان الملف، وزرا Excel/PDF بالواجهة.
  4. **المرجع:** صار `HW-000001` متسلسلاً لكل مستأجر (فرادة على مستوى المستأجر بدل العالمية) — القديم يبقى بصيغته.
  5. **الكروت/الجداول:** مكوّن مركزي `ViewToggle + useViewMode` — على الجوال الافتراضي **كروت** (لا سحب أفقياً) وعلى الواسع جدول، والتبديل بزر — طُبّق على السجلين، وإجراءات الكبير مستخرجة لدالة واحدة تعمل بالعرضين.
  6. حُذفت تلميحات البريد الثلاثة.
- **الفحص:** 147 اختباراً ✅ (مرجع بصيغة جديدة + تصدير) + بناء و lint ✅ + لقطات: الكود بالتوب بار، لوغو مرفوع بالشعار، أزرار التصدير والتبديل، وجوال 390px كروتاً كاملة.

## ملاحظة 16 — أرقام نظيفة بكل النظام + سبب إلزامي + صفحة تفاصيل الصندوق (2026-08-08)

- **الملاحظة:** «الأصفار الزائدة بكل المشروع + الاعتماد والسحب يجب أن يُكتب سبب واضح + الصناديق ذكية: الضغط يأخذنا **لصفحة تفاصيل** فيها كل حركات الصندوق وفلاتر (التاريخ/المكتب/سحب/اعتماد/تسوية) + لنا وله + زر تسوية للرصيد السابق».
- **الإصلاح:**
  1. **`fmt` مركزي بالخادم** (`core/format.py`): كل مبلغ في نص مولّد — إشعارات، رسائل الواتساب/البوت، ملفات التصدير، PDF المطابقة — بلا أصفار زائدة. وقوالب واتساب الواجهة صارت تمرّ بـ`formatMoney`.
  2. **السبب إلزامي:** الاعتماد/السحب يرفضان بلا سبب (400)، والسبب يُدمج في بيان القيد («اعتماد باسم فلان: تعزيز رصيد») فيظهر بالكشوف والتدقيق.
  3. **تسوية الصندوق:** خدمة `adjust_box` (إضافة/خصم بسبب إلزامي) مقابل حساب «تسويات الصناديق» الجديد (Kind ADJUSTMENT + هجرة) — لتصحيح رصيد سابق أو فرق جرد.
  4. **صفحة تفاصيل الصندوق** `/office/boxes/[id]`: رصيدنا (لنا/له علينا) + داخل/خارج للفترة، أزرار العملات، فلاتر النوع (اعتماد/سحب/تسوية/حركات/عكس) والمكتب والتاريخ، الحركات مصنّفة بشارات مع صاحبها وسببها (جدول/كروت بالزر الذكي)، وأزرار اعتماد/سحب/تسوية من الصفحة نفسها. كرت الصندوق في القائمة صار يفتحها، وأُزيلت النوافذ القديمة.
  5. نقطة `movements` جديدة (تصنيف قيود + فلترة entry-level بالمكتب) و`adjust` مدقَّقة.
- **الفحص:** 3 اختبارات جديدة (سبب إلزامي 400 + تسوية بالاتجاهين وأثرها بالرصيد + التصنيف وفلترا المكتب والنوع) — 152 اختباراً ✅ + بناء و lint ✅ + فحص حي: تسوية 500 «رصيد افتتاحي» ظهرت مصنّفة والرصيد 1,840 لنا صحيح.

## ملاحظة 17 — حذف رئيسية المكتب الصغير (2026-08-08)

- **الملاحظة:** «المكاتب الصغيرة صفحة الرئيسية بلا فائدة احذفها».
- **الإصلاح:** حُذف بند «الرئيسية» من قائمة الصغير ومحتوى الصفحة (أرصدتها في «الصناديق» وآخر الحركات في «سجل الحركات» أصلاً)؛ الدخول صار يهبط مباشرة على **«إرسال حركة»** — جوهر عمل الصغير، و`/small` يحوّل إليها تلقائياً لأي رابط قديم.
- **الفحص:** بناء و lint ✅.

## ملاحظة 18 — إظهار حالة التسليم في سجل الصغير (2026-08-08)

- **الملاحظة:** «ذكرت سابقاً أن الصغير يجب أن يعرف إذا تم تسليم الحوالة — لكنها غير مطبّقة».
- **التشخيص:** إشعار التسليم (ملاحظة 7) مطبّق ويعمل — أُثبت بفحص حي شامل (إرسال → قبول → تسليم → وصل الإشعار «سُلّمت حركتك HW-000006» للصغير). الثغرة الفعلية: **سجل حركات الصغير لا يعرض حالة التسليم** — لا عمود بالجدول ولا شارة بالكروت، فلا يرى الأثر الدائم.
- **الإصلاح:** عمود «التسليم» في جدول سجل الصغير + شارة «تم التسليم» في كرت الحركة بجانب «مدفوعة».
- **الفحص:** فحص حي end-to-end للإشعار عبر الـ API + لقطة السجل بالشارة الجديدة + بناء و lint ✅.

## ملاحظة 20 — عملة الأجور + الكود تحت الاسم + تاريخ بلا ساعة (2026-08-08)

- **الملاحظة:** «الأجور تكون من نفس عملة المبلغ (تأتي من الكبير) — حدد العملة + ضع كود المكتب تحت اسم المكتب + سجل الحوالات: الساعة غير مهمة، فقط التاريخ».
- **الإصلاح:**
  - **عملة الأجور:** نعم — الأجور دائماً بعملة المبلغ المقبوضة (قرار موثّق منذ الدورة §4-أ). صارت **ظاهرة**: عمود الأجور في السجلين يعرض العملة، وحقلا الأجور في شاشة المعالجة يحملان العملة في العنوان + سطر توضيحي.
  - **الكود تحت الاسم:** شارة الكود في التوب بار انتقلت لسطر صغير تحت اسم المكتب.
  - **التاريخ فقط:** دالة مركزية جديدة `formatDate` — سجلا الحوالات (جدولاً وكروتاً) والتصدير Excel/PDF بالتاريخ فقط بلا ساعة. (الساعة باقية حيث تهم: الإشعارات، المطابقات، كشف الصندوق.)
- **الفحص:** بناء و lint ✅ + اختبارات التصدير ✅.

## ملاحظة 48 — استيعاب «صحوة» الخادم المجاني + نبضة إبقاء + لا فشل صامت (2026-08-08)

- **الملاحظة:** ظهر الخطأ الحقيقي أخيراً: «تعذر الاتصال بخادم الواتساب: Read timed out (timeout=20)» — خدمة WAHA المجانية تنام بعد 15 دقيقة خمول وصحوتها ~50 ثانية، أطول من مهلتنا.
- **الإصلاح:**
  - **مهلة موحّدة 75 ثانية** لكل نداءات WAHA (تستوعب الصحوة كاملة).
  - **نبضة إبقاء**: مهمة Celery Beat كل 10 دقائق تنادي `/ping` فلا ينام الخادم أصلاً.
  - **مؤشر «جارٍ الإرسال…»** على زر إرسال المطابقة (قد يستغرق الإيقاظ الأول دقيقة).
  - **لا فشل صامت في المسار اليدوي**: `sendToWhatsApp` يكشف منع المتصفح للنوافذ المنبثقة ويعيد `blocked` — وكل مسار يعرض إرشاداً واضحاً (نُسخ والصق / مُنعت النافذة / فُتحت المشاركة) في ملف العضو وشاشة إرسال الصغير.
- **الفحص:** كامل الحزمة ✅ + بناء و lint ✅.

## ملاحظة 47 — إرسال فوري ضمن الطلب + إظهار سبب الفشل (2026-08-08)

- **الملاحظة:** «ما وصل شي أبداً — لا بالخاص ولا بالمجموعة» رغم أن الرقم مربوط ✓.
- **التشخيص:** الإرسال كان يُترك كلياً لعامل الخلفية (Celery) — وعلى خدمة Render المجانية الواحدة قد يكون العامل ميتاً بلا أي أثر ظاهر، فتبقى الرسائل «قيد الإرسال» صامتة إلى الأبد.
- **الإصلاح:**
  - **محاولة فورية ضمن الطلب نفسه:** `queue_message` يرسل عبر WAHA مباشرة — نجحت؟ الحالة «أُرسلت» فوراً. فشلت؟ تُسجَّل رسالة الخطأ وتُترك للطابور ليعيد المحاولة.
  - **الواجهة تُظهر الحقيقة:** «إرسال مطابقة» يعرض «أُرسلت عبر الواتساب ✓» فقط عند التسليم الفعلي — وإلا يعرض **رسالة الخطأ الحقيقية** من الخادم بدل الصمت.
- **الفحص:** كامل الحزمة ✅ + بناء و lint ✅.

## ملاحظة 46 — الإرسال المباشر يصل للمجموعة عبر رابطها (2026-08-08)

- **الملاحظة:** «ما وصلت الرسالة إلى المجموعة وقت أرسلت مطابقة» — التوقع أن تصل رسالة المطابقة للمجموعة نفسها.
- **الإصلاح:** صار النظام يستنتج **معرّف المجموعة من رابط الدعوة** المحفوظ للعضو (عبر `groups/join-info` في WAHA) ويخزّنه تلقائياً — فأولوية وجهة الإرسال المباشر: **المجموعة** (معرّف مخزَّن أو مستنتَج من رابطها) ثم رقم هاتف العضو. يشترط أن يكون الرقم المربوط عضواً في المجموعة.
- **تنبيه للوضع اليدوي:** بلا ربط رقم، «إرسال مطابقة» ينسخ النص ويفتح المجموعة — واللصق والإرسال على المستخدم (لا إرسال ذاتياً في الوضع اليدوي).
- **الفحص:** اختبار جديد (استنتاج المعرّف من الرابط + تخزينه + وصول الرسالة للمجموعة) — كامل الحزمة ✅.

## ملاحظة 45 — حذف سجل الرسائل + WAHA مجاني على الديمو + توضيح المجانية (2026-08-08)

- **الملاحظة:** «ما فيني أربط واتساب شو استفدت؟ وسجل الرسائل ما بدي ياه أبداً» + «ما عندي API — بدي الطريقة المجانية وخليها موجودة احتياط».
- **الإصلاح:**
  - **سجل الرسائل المُرسلة حُذف** من إعدادات المكتب كلياً (نقطة الخادم باقية للتشخيص الداخلي فقط).
  - **خادم WAHA أُضيف لمخطط Render المجاني** (`hawalat-waha-demo`، محرك NOWEB الخفيف بلا متصفح) — فصار الربط بمسح QR قابلاً للتجربة فعلياً على الديمو، كله على الخطة المجانية.
  - **توضيح المجانية في الواجهة:** WAHA مفتوح المصدر يعمل كـ«واتساب ويب» — لا API رسمياً ولا اشتراك؛ «مفتاح الخادم» أعيدت تسميته «كلمة سر الخادم» مع شرح أنها من اختيار الأدمن. والطريقة اليدوية بالرابط تبقى احتياطاً تلقائياً دائماً متى كان الرقم غير مربوط.
- **الفحص:** بناء و lint ✅.

## ملاحظة 44 — نظام الواتساب الجديد: ربط رقم المكتب بمسح QR (2026-08-08)

- **الملاحظة:** «نغير نظام البوت: المكتب الكبير يربط رقمه على الواتساب بحيث وقت يرسل مطابقة تنزل مباشرة بدون الحاجة لفتح رابط».
- **البنية الجديدة:**
  - **خادم WAHA واحد للمنصة** يضبطه الأدمن مرة واحدة (عنوان + مفتاح — بطاقة جديدة في إعدادات الأدمن)، وجلسة مستقلة لكل مستأجر `hawalat-{id}`.
  - **ربط بمسح QR:** بطاقة «ربط الواتساب» في إعدادات المكتب الكبير: زر «ربط رقمي الآن» → QR يظهر ويتجدد تلقائياً (تحديث كل 4 ثوانٍ) → بعد المسح تظهر «مربوط +الرقم» وزر فك الربط. الرقم يُخزَّن في `linked_number`.
  - **الإرسال المباشر:** «إرسال مطابقة» وأي حركة جديدة تُرسل فوراً من رقم المكتب عبر الجلسة — والواجهة تعرض «أُرسلت عبر الواتساب ✓» بلا فتح أي رابط. إن لم يكن الرقم مربوطاً يبقى الرابط اليدوي كما كان.
  - **وجهة الإرسال:** مجموعة المكتب الصغير إن ضُبطت، وإلا **رقم هاتفه مباشرة** (`digits@c.us`) — ولهذا لم يعد معرّف البوت ضرورياً (ملاحظة 39).
  - Backend: `waha.py` عميل كامل (إنشاء/حالة/QR/خروج/إرسال + مفتاح X-Api-Key) + `WhatsAppLinkView` (GET/POST/DELETE بتدقيق) + `is_ready` = خادم مضبوط + رقم مربوط + هجرتا `waha_settings` و`linked_number`. أزيل PATCH إعدادات البوابة القديم.
- **الفحص:** 13 اختبار واتساب معاد كتابتها (ربط QR وهمي، تخزين الرقم، فك الربط، الإرسال بمفتاح الخادم، السقوط للهاتف، الفشل بعد المحاولات، العزل) — كامل الحزمة 157 ✅ + بناء و lint ✅ + لقطة حيّة لبطاقة الربط.
- **للنشر:** يلزم تشغيل حاوية WAHA وضبط عنوانها ومفتاحها من إعدادات الأدمن — موثق في دليل النشر.

## ملاحظة 43 — رموز مضمونة العرض في رسالة المطابقة (2026-08-08)

- **الملاحظة:** الرسالة وصلت � — «لا أعلام ولا غيرو ولا بطيخ».
- **التشخيص:** أعلام الدول و🌟💎 من فئة الإيموجي «العليا» (خارج BMP) — تنكسر إلى � على بعض الأنظمة أثناء نقل النص. النقاط والتاريخ (رموز أساسية) وصلوا سليمين — وهذا أكد التشخيص.
- **الإصلاح:** استُبدلت كلها برموز أساسية مضمونة على كل الأجهزة: إشارة العملة الرسمية ($ € ₺ £ ل.س ﷼ …والافتراضي ¤) بدل العلم، ⭐ بدل 🌟، ✨ بدل 💎 — و⌛⏳ أصلاً آمنتان.
- **الفحص:** بناء و lint ✅.

## ملاحظة 42 — الرصيد الصفري لا يُذكر في رسالة المطابقة (2026-08-08)

- **الملاحظة:** «ما في داعي للمتوازن — هيك يكون الرصيد صفر، ما في داعي ينذكر أساساً».
- **الإصلاح:** رسالة المطابقة تعرض فقط العملات ذات رصيد فعلي (لنا/لكم) — الصفرية تُحذف من الرسالة كلياً.
- **الفحص:** بناء و lint ✅.

## ملاحظة 41 — قالب رسالة المطابقة بالواتساب بالشكل المعتمد (2026-08-08)

- **الملاحظة:** حدّد صاحب المشروع شكل الرسالة حرفياً: «🌟مطاااااااابقة🌟 / حتى تاريخ هذه اللحظة / فاصل نقاط / كل عملة مع العلم مع المبلغ إذا لنا أو لكم / فاصل / يرجى تأكيد المطابقة / ⌛️⌛️💎💎💎💎⏳⏳».
- **الإصلاح:** `buildReconciliationMessage` أعيدت كتابته على القالب حرفياً: عنوان النجوم، تاريخ اللحظة الحالية، فاصلا النقاط، سطر لكل عملة بعلمها (🇺🇸🇹🇷🇸🇾…والافتراضي 💱) والمبلغ + لنا/لكم (والمتوازنة «متوازن ✅»، والعملات الخاملة كلياً تُحذف)، ثم طلب التأكيد والخاتمة. أُلغيت معاملات الاسم/الكود/آخر مطابقة من التوقيع.
- **الفحص:** بناء و lint ✅.

## ملاحظة 40 — إيقاف مؤقت للمكتب الصغير: يدخل ولا يرسل (2026-08-08)

- **الملاحظة:** «لازم في خيار إيقاف مؤقت بحيث يتجمد حسابه — يدخل ولكن ما يقدر يرسل أي حركة».
- **الإصلاح:**
  - **Backend:** حقل `is_suspended` على المستخدم (هجرة 0005) + نقطتا `suspend`/`unsuspend` في الحسابات (مع تدقيق) — وإنشاء الحركة يرفض الموقوف مؤقتاً 403 برسالة «حسابك موقوف مؤقتاً عن إرسال الحركات — تواصل مع مكتبك». الدخول والمشاهدة والصناديق تبقى كلها تعمل (بخلاف الحظر الذي يمنع الدخول كلياً).
  - **Frontend:** زر «إيقاف مؤقت/تشغيل» في ملف المكتب + شارة «موقوف مؤقتاً» الكهرمانية في الملف والقائمة (محظور > موقوف > نشط).
  - اختبار E2E: إيقاف → دخول ناجح + قائمة تعمل + إرسال 403 → تشغيل → إرسال 201.
- **الفحص:** كامل الحزمة ✅ + بناء و lint ✅.

## ملاحظة 39 — حذف معرّف مجموعة البوت من الواجهة (2026-08-08)

- **الملاحظة:** «احذف معرف البوت ماله داعي أصلاً».
- **الإصلاح:** أزيل حقل «معرّف مجموعة البوت» من فورم فتح المكتب ونافذة تعديل البيانات وبطاقة بيانات الملف. (الحقل باقٍ في قاعدة البيانات — إن فُعّل وضع البوت عند النشر يُعاد ربطه من الخادم بلا واجهة.)
- **الفحص:** بناء و lint ✅.

## ملاحظة 38 — اسم المكتب هو الرابط: لا زر «الملف الكامل» (2026-08-08)

- **الملاحظة:** «مو احترافي تكتب الملف كامل — خلص نضغط على اسم المكتب وندخل».
- **الإصلاح:** حُذف زر/عمود «الملف الكامل» من جدول الحسابات وكروتها — الاسم الملوّن هو الرابط.
- **الفحص:** بناء و lint ✅ + لقطة حيّة.

## ملاحظة 37 — المبلغ تحت المستفيد في الكروت (2026-08-08)

- **الملاحظة:** «المبلغ لازم تحت المستفيد».
- **الإصلاح:** ترتيب حقول الكرت صار: من مكتب → (المرسِل) → المستفيد → **المبلغ** → الوجهة… في سجل الكبير وسجل الصغير والحركات الجارية.
- **الفحص:** بناء و lint ✅ + لقطة حيّة.

## ملاحظة 36 — لا فراغ بعد عمود الإجراءات (2026-08-08)

- **الملاحظة:** «في فراغ آخر شي بالإجراءات — شكل الجدول شوي مو احترافي».
- **الإصلاح:** عمود الإجراءات صار بأدنى عرضه (`w-px`) فتتوزع المساحة الفائضة على أعمدة النصوص بدل التكدس فراغاً بآخر الجدول، والصفوف بلا إجراء تعرض شرطة باهتة بدل الخواء. وأصلح `Table` المركزي: `min-w-max` بدل 640px ثابتة — فلا قصّ داخل البطاقات الضيقة (جدول مطابقة الملف).
- **الفحص:** بناء و lint ✅ + لقطة حيّة.

## ملاحظة 35 — كروت حركات الصندوق بحقول وأيقونات (2026-08-08)

- **الملاحظة:** «الكروت هون تفاصيل مع أيقونات نفس السابقات» — على صفحة تفاصيل الصندوق.
- **الإصلاح:** كرت حركة الصندوق صار بنمط `TxnField` الموحّد: شارة النوع أعلى، ثم حقول مفصولة بأيقونات (المبلغ داخل/خارج ملوّناً، المكتب، البيان، التاريخ).
- **الفحص:** بناء و lint ✅ + لقطة حيّة.

## ملاحظة 34 — الحسابات: كروت + ملف فردي كامل لكل مكتب صغير (2026-08-08)

- **الملاحظة:** «الحسابات أيضاً طبّق عليها نظام الكروت — واتفقنا: الضغط على اسم المكتب يفتح صفحة فيها كل تفاصيل المكتب بشكل فردي وكامل».
- **الإصلاح:**
  - **القائمة:** زر جدول/كروت (كروت بحقول وأيقونات) + باجينيشن — والاسم في الوضعين رابط إلى الملف، وكل صف/كرت فيه زر «الملف الكامل». أزيلت أزرار الإجراءات المبعثرة من القائمة (كنمط الصناديق).
  - **صفحة الملف الفردي `/office/members/[id]`:** بيانات المكتب كاملة بأيقونات + **تعديل البيانات** (اسم/هاتف/بريد/واتساب — نافذة PATCH جديدة) + الأرصدة والمطابقة (معاينة حيّة، إرسال واتساب، تثبيت نقطة إغلاق) + **كشف الحساب بالعملة** (نوع/بيان/عليه/له/تاريخ + باجينيشن) + **سجل المطابقات المثبّتة** مع تنزيل PDF + الحدود + استعادة كلمة المرور + حظر/فك — كل ما يخص العضو في مكان واحد.
  - **Backend:** أُضيف `retrieve` لعضو مفرد في `MembersViewSet` (ضمن المستأجر حصراً — الغريب 404) + اختباران.
- **الفحص:** كامل حزمة الاختبارات ✅ + بناء و lint ✅ + لقطات حيّة للقائمة والملف.

## ملاحظة 33 — أزرار الجدول مكتوبة بسطر واحد + عمود مرجع مرصوف (2026-08-08)

- **الملاحظة:** «رجّع الأزرار كتابة — هيك مو مفهومة. وحط المرجع فوق والتاريخ تحت وصغّر العمود لناخذ مساحة أكبر».
- **الإصلاح:** ألغيت الأزرار الأيقونية في الجدول: الأزرار المسماة عادت (مدفوعة/تم التسليم/تعديل/عكس) بسطر واحد بلا التفاف وبحجم مضغوط، وخُفّف حشو خلايا هذا الجدول ليتسع الكل بلا قص. عمود المرجع صار بعرضه الأدنى: المرجع فوق والتاريخ سطر صغير تحته بمحاذاة واحدة.
- **الفحص:** بناء و lint ✅ + لقطة حيّة: كل الأزرار الأربعة ظاهرة بسطر واحد داخل الشاشة.

## ملاحظة 32 — المبلغ حقل ضمن الكرت لا عنواناً فوقه (2026-08-08)

- **الملاحظة:** «غلط كتابتك المبلغ بهذا الشكل — لازم يكون ضمن الكرت».
- **الإصلاح:** المبلغ انتقل من سطر كبير فوق الحقول إلى **أول حقل** داخل حاوية الحقول بأيقونة 💵 وقيمة عريضة — في كروت سجل الكبير وسجل الصغير والحركات الجارية معاً.
- **الفحص:** بناء و lint ✅ + لقطة حيّة.

## ملاحظة 31 — صح أخضر بدل شارات «مدفوعة/تم التسليم» في الجداول (2026-08-08)

- **الملاحظة:** «بدل ما نكتب مدفوعة نحط إشارة صح تحت العمود، وتظهر فقط إذا ضغطنا زره — مثلاً ضغطنا تم التسليم يظهر صح أخضر تحت العمود».
- **الإصلاح:** مكوّن مركزي جديد `DoneCheck`: صح أخضر دائري يظهر فقط بعد تنفيذ الإجراء، وقبله شرطة باهتة. طُبّق في جدولي السجل (الكبير والصغير): عمودا «مدفوعة» و«التسليم» أيقونيان نحيلان بمحاذاة وسط، وعمود «الحالة» بقي لشارة القبول وحدها. الكروت أبقت شاراتها المسماة (عندها متسع).
- **الفحص:** بناء و lint ✅ + لقطة حيّة: الصح الأخضر يظهر للمدفوعة والمسلَّمة فقط.

## ملاحظة 30 — إصلاح جدول سجل حركات الكبير: أعمدة مدموجة وأزرار أيقونية (2026-08-08)

- **الملاحظة:** «شوف شكل الجدول مبهدل كثير وفايت ببعضه والأزرار سطرين بشعة» — بدء جولة اختبار المكتب الكبير.
- **التشخيص:** جدول سجل الكبير كان 11 عموداً (تاريخ منفصل + 3 أعمدة حالات منفصلة) فتتزاحم الخلايا، وأزرار الإجراءات المسماة تلتف على سطرين.
- **الإصلاح:**
  - **دمج الأعمدة إلى 8:** المرجع والتاريخ متراصّان في خلية واحدة (المرجع فوق والتاريخ سطر صغير تحته)، وأعمدة القبول/الدفع/التسليم الثلاثة دُمجت في عمود «الحالة» واحد تظهر فيه الشارات معاً.
  - **أزرار أيقونية بسطر واحد:** `rowActions(t, true)` وضع مضغوط للجدول — مربعات أيقونية صغيرة بحدود (مدفوعة/تم التسليم/تعديل، والعكس بأحمر) والاسم tooltip، بلا التفاف. الكروت تبقى بالأزرار المسماة.
  - `whitespace-nowrap` على الأعمدة الرقمية والقصيرة حتى لا تنكسر القيم.
  - المسلَّمة تعرض «سُلّمت — نهائي» بسطر واحد ثابت.
- **الفحص:** بناء و lint ✅ + لقطة حيّة: الجدول مرتّب بلا تزاحم والأزرار سطر واحد.

## ملاحظة 29 — إزالة إشارات ± من كل الواجهة (2026-08-08)

- **الملاحظة:** «ما في داعي للزائد والناقص — المنطق الحسابي معروف: لنا واضح ولكم واضح، الإشارة مزعجة».
- **الإصلاح:** `formatMoney` المركزي صار يعرض القيمة المطلقة دائماً بلا إشارة — الألوان (أخضر/أحمر) والتسميات (لنا/لكم، لك/عليك) تحمل المعنى، فانعكس على كل الشاشات دفعة واحدة. وأزيلت + و− الصريحتان من كروت الكشف وحركات الصندوق (استُبدلتا بكلمتي داخل/خارج حيث يلزم). **الاستثناء المقصود:** مستندات PDF/Excel المولّدة من الخادم تُبقي الإشارة — لا ألوان فيها، فالإشارة هناك هي حامل المعنى.
- **الفحص:** بناء و lint ✅.

## ملاحظة 28 — شريط سفلي كأندرويد + إلغاء الجداول على الجوال (2026-08-08)

- **الملاحظة:** «عند فتحه من المتصفح يثبّت كتطبيق أندرويد (موجود ✓ — PWA بوضع standalone) + أزرار من تحت كبرنامج أندرويد نظامي + نلغي الجداول بعرض الجوال لأن السكرول الأفقي مزعج».
- **الإصلاح:**
  - **شريط تنقّل سفلي** على الجوال (fixed bottom + safe-area): أول 4 أقسام أيقونة واسماً، والنشط بحبة ملونة — وزر «المزيد» يفتح اللوح المنسدل ببقية الأقسام والتثبيت والخروج. حُذف زر ☰ العلوي (المزيد يغني عنه) وأُضيف متسع سفلي للمحتوى.
  - **الجداول ملغاة على الجوال نهائياً:** `useViewMode` يفرض الكروت تحت 768px (مع مستمع resize — حتى تدوير الشاشة لا يعيد الجدول)، وزر التبديل نفسه مخفي على الجوال.
- **الفحص:** بناء و lint ✅ + فحص حي بعرض 390px: الشريط السفلي يظهر والتنقّل منه يعمل (URL تأكد) والكروت مفروضة.

## ملاحظة 27 — كشف الصندوق يتبع زر الكروت أيضاً (2026-08-08)

- **الملاحظة:** «بالصناديق الكشف أيضاً يجب أن يتحول لنظام الكروت عند الاختيار من الأعلى — وليس فقط الصناديق».
- **الإصلاح:** الكشف التفصيلي «لنا/لكم» صار يتبع زر التبديل نفسه: كروت (النوع + التاريخ + المبلغ الملون لنا/لكم + البيان) أو جدول — والباجينيشن يعمل بالوضعين.
- **الفحص:** بناء و lint ✅ + لقطة حيّة للكشف كروتاً.

## ملاحظة 26 — تجاوب الجوال الكامل: قائمة منسدلة + كروت المعالجة (2026-08-08)

- **الملاحظة:** «غير متجاوب مع كل الشاشات — بالجوال لا يمكنني الوصول إلى كل الأقسام والبيانات + بعرض الجوال النظام بالكروت لا الجداول».
- **التشخيص:** السايدبار كان `hidden md:flex` **بلا أي بديل على الجوال** — لا قائمة إطلاقاً. هذه كانت الثغرة الجوهرية.
- **الإصلاح:**
  - **قائمة جوال منسدلة** في Shell: زر ☰ بالتوب بار (يظهر على الصغيرة فقط) يفتح لوحاً جانبياً فيه اللوغو وكل الأقسام (نفس روابط السايدبار — دالة واحدة مشتركة) + تثبيت التطبيق وتسجيل الخروج، يُغلق بالضغط خارجه أو بالتنقّل.
  - **الحركات الجارية (المعالجة)** حصلت على كروت + زر التبديل — آخر جدول أساسي بلا كروت؛ الكرت يحمل كل الحقول بأيقونات وزر «معالجة».
  - بقية الجداول الثانوية (أدمن/حسابات) تتمرر أفقياً داخل إطارها بأمان (مكوّن Table المركزي).
- **الفحص:** بناء و lint ✅ + فحص حي بعرض 390px: القائمة تفتح وتعرض كل الأقسام والتنقّل يعمل والكروت افتراضية بلا سحب أفقي.

## ملاحظة 25 — جدول/كروت في صناديق الصغير ومطابقاته (2026-08-08)

- **الملاحظة:** «بالمكاتب الصغيرة الصناديق أيضاً تكون نظام جدول أو كروت + بالمطابقات نفس الشيء».
- **الإصلاح:** زر التبديل المركزي `ViewToggle` أُضيف إلى:
  - **صناديق الصغير:** كروت (الملونة الذكية) أو جدول (العملة/لك/عليك/الصافي + زر الكشف) — الكشف التفصيلي يعمل من الوضعين.
  - **المطابقات:** كروت (القابلة للفتح) أو جدول (التاريخ/ثبّتها/الأرصدة المثبّتة/إجراءات: التفاصيل + PDF) — لوح لقطة التثبيت يفتح تحت الجدول.
  - على الجوال يبقى الافتراضي كروتاً (بلا سحب أفقي) في الصفحتين تلقائياً.
- **الفحص:** بناء و lint ✅ + لقطتان حيّتان للوضع الجدولي في الصفحتين.

## ملاحظة 24 — لوغو أوسط طبيعي بخط فاصل + باجينيشن مركزي (2026-08-08)

- **الملاحظة:** «السايدبار مفصول بخط واللوغو بالوسط أكبر وأوضح، بلا حدود ولا قص ولا دائرة + باجينيشن بالصفحات اللازمة مثل الحركات والصناديق».
- **الإصلاح:**
  - **اللوغو:** `PlatformLogo` صار بأحجام (`md/lg/xl`) والصورة تُعرض **على طبيعتها** (`object-contain` بلا rounded ولا إطار) — الشعار الجانبي بالوسط بحجم `lg` وتحته خط فاصل، والدخول `xl`. (الافتراضي «ح» يبقى مربع الهوية إن لم يُرفع لوغو.)
  - **باجينيشن مركزي:** `usePagination + Pagination` (سابق/تالي + «صفحة س من ص · N سجلاً»، يعود للأولى عند تغيّر الفلاتر) — طُبّق على: سجل الحركات (الصغير والكبير، جدولاً وكروتاً)، حركات صفحة الصندوق، وكشف الصغير التفصيلي.
- **الفحص:** بناء و lint ✅ + لقطة السايدبار الجديد.

## ملاحظة 23 — صقل كرت الحركة: مرجع أعلى اليسار وحقول مفصولة محاذاة (2026-08-08)

- **الملاحظة:** «رقم الحركة فوق على اليسار + كل معلومة بحقل مفصول + زبط المحاذاة يمين/يسار».
- **الإصلاح:** الصف العلوي: الحالات يميناً والمرجع أقصى اليسار (`ms-auto`)؛ والحقول صارت صفوفاً مفصولة بخطوط داخل إطار خفيف (`divide-y` + خلفية هادئة) — **الاسم والأيقونة يميناً والقيمة يساراً** بمحاذاة ثابتة، كله عبر `TxnField` المركزي فانعكس على السجلين معاً.
- **الفحص:** بناء و lint ✅ + لقطة حيّة.

## ملاحظة 22 — كشف الصندوق بنوع مصنّف وبيان نظيف (2026-08-08)

- **الملاحظة:** «الجدول يجب أن يحوي النوع (حوالة/اعتماد/ملغية…) + البيان غلط — يكون الرقم المرجعي أو ملاحظة مثل اعتماد على صندوق كذا + التاريخ بدون ساعة».
- **الإصلاح:**
  - **تصنيف مركزي** في خدمة الكشف `classify_statement_line`: كل سطر يحمل `kind` (حوالة/اعتماد/سحب/قبض/عكس-ملغاة/تسوية) و`note` نظيفاً — **المرجع** للحوالات والقبض والعكس، و**الملاحظة** للتسويات.
  - بيان الاعتماد/السحب صار يتضمن الصندوق: «في صندوق الوعد — تعزيز رصيد».
  - كشف الصغير: عمود «النوع» بشارات ملونة + «البيان» النظيف + التاريخ بلا ساعة.
- **الفحص:** 152 اختباراً ✅ + بناء و lint ✅ + لقطة حيّة: حوالة ← HW-000006، اعتماد ← ملاحظته، والتواريخ بلا ساعات.

## ملاحظة 21 — إعادة تصميم كرت الحركة (2026-08-08)

- **الملاحظة:** «الكرت جميل ولكن الحالات أفقياً بالأعلى + رقم الحركة بالسطر التالي + البيانات بحقول مع أيقونات معبّرة».
- **الإصلاح:** مكوّن مركزي جديد `TxnField` (أيقونة بلون الهوية + اسم + قيمة) — كرت الحركة في السجلين صار: صف الحالات أفقياً بالأعلى ← المرجع بسطر مستقل ← المبلغ بارزاً ← حقول بأيقونات (مستفيد/مرسِل/وجهة/أجور/تاريخ — وللكبير: من مكتب/صندوق أيضاً) ← إجراءات الكبير.
- **الفحص:** بناء و lint ✅ + لقطة حيّة للكرت الجديد بحالاته الثلاث.

## ملاحظة 19 — اللوغو فقط بلا كلمة «حوالات» (2026-08-08)

- **الملاحظة:** «احذف كلمة حوالات نهائياً واترك فقط اللوغو يكفي».
- **الإصلاح:** الشعار الجانبي صار اللوغو وحده (بحجم أكبر قليلاً)، وكذلك ترويسة شاشة الدخول — الاسم النصي حُذف من الاثنين. (عنوان تبويب المتصفح يبقى «حوالات» — ليس عنصراً بصرياً بالواجهة.)
- **الفحص:** بناء و lint ✅.

## قالب المراحل القادمة

```markdown
## المرحلة N — الاسم

- **الحالة:** 🟡 قيد التنفيذ / ✅ مقبولة ومغلقة / 🔴 مرفوضة (يُعاد الفحص)
- **الهدف المعلن:** (من الخطة)

### ما أُنجز
- ...

### فحص القبول
| القاعدة | النتيجة | الدليل |
|---|---|---|
| ق1..ق10 | ✅/🔴/➖ | دليل ملموس (أمر نُفّذ، ملف، اختبار) |
+ شروط خاصة بالمرحلة نفسها (من مخرَجها المعلن في الخطة)

### ملاحظات وديون مؤجلة
- ...
```

> **قاعدة صارمة:** أي 🔴 في الفحص = المرحلة لا تُغلق حتى يُصحَّح ويُعاد الفحص.
> أي ➖ يجب أن يُذكر متى سيبدأ انطباقه.
